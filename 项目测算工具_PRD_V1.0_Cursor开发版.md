# 项目测算工具 PRD V1.0

> **用途**：用于 Cursor 直接搭建第一版完整 Demo\
> **产品形态**：嵌入现有项目管理系统的「项目测算」模块\
> **核心目标**：将《重卡运力测算模型-V5》从 Excel
> 黑盒模型重构为标准化、可配置、可追溯、可版本化的项目经营测算工具。\
> **V1原则**：先完整跑通"建方案 → 填参数 → 自动测算 → 看结果 →
> 做敏感性分析 → 保存版本"的闭环；不照搬 Excel 页面结构。

------------------------------------------------------------------------

## 1. 产品背景

目前重卡运输项目的经济性测算主要依赖 Excel。现有 Excel
同时承担参数录入、收入计算、成本计算、税费计算、现金流、IRR、多线路汇总及敏感性分析。

主要问题：

1.  公式散落在大量单元格中，业务人员难以理解和维护。
2.  多线路通过复制 Sheet 实现，现模型存在固定线路数、固定路段数的 Excel
    结构限制。
3.  项目级、线路级、路段级、车辆级参数混杂。
4.  标准参数和项目特有参数混杂，不利于统一测算口径。
5.  参数被修改后缺乏完整版本和变更留痕。
6.  无法方便地比较不同商务、运营、能源方案。
7.  测算结果与项目管理、后续实际运营数据割裂。
8.  Excel
    中部分业务规则尚未完全确认，因此系统必须支持规则配置和版本化，不能将所有数值硬编码。

------------------------------------------------------------------------

## 2. 产品定位

项目测算工具是项目管理系统中的经营测算能力。

标准业务链路：

`项目 → 新建测算方案 → 配置线路/路段 → 配置运力 → 配置商务与运营参数 → 配置成本/财税参数 → 执行测算 → 查看经营结果 → 查看现金流 → 敏感性分析 → 保存版本 → 设为基准方案`

后续预留：

`基准测算 → 项目立项 → 项目执行 → TMS/车联网实际数据回流 → 测算 VS 实际`

------------------------------------------------------------------------

## 3. V1 产品目标

### 3.1 必须实现

-   一个项目可以建立多个测算方案。
-   一个方案可以动态新增任意数量线路。
-   一条线路可以动态新增任意数量路段。
-   参数按照项目/方案/线路/路段/车辆/公共标准分层。
-   标准参数可自动带入，项目参数允许调整。
-   系统统一执行收入、成本、利润、现金流计算。
-   支持月度结果和项目汇总结果。
-   支持关键参数敏感性分析。
-   支持方案复制和版本管理。
-   支持设置一个基准测算版本。
-   所有核心计算结果可追溯到输入参数。
-   对缺失参数、非法参数、除零等情况进行校验，不向用户展示 Excel 式
    `#DIV/0!`。

### 3.2 V1 暂不强制

-   TMS 实际运营数据自动回流。
-   车联网历史能耗自动推荐。
-   自动获取地图导航里程。
-   合同/计费规则自动引用。
-   完整审批流。
-   AI 自动生成测算建议。
-   多人实时协同编辑。

以上能力保留接口。

------------------------------------------------------------------------

## 4. 核心数据对象

### 4.1 Project 项目

建议直接引用现有项目管理系统 Project，不重复创建项目主数据。

核心字段：

-   project_id
-   project_code
-   project_name
-   customer_name
-   project_manager
-   project_status
-   start_date
-   end_date

### 4.2 CalculationScheme 测算方案

字段：

-   id
-   project_id
-   scheme_name
-   scheme_code
-   version_no
-   status：draft/calculated/baseline/archived
-   description
-   lease_type
-   fleet_size
-   calculation_years
-   created_by
-   created_at
-   updated_by
-   updated_at
-   source_scheme_id
-   rule_version_id

规则：

-   同项目允许多个方案。
-   一个方案允许产生多个版本。
-   同项目同一时点只允许一个"基准方案"。
-   已设为基准的版本原则上不可直接覆盖修改；修改时复制生成新版本。

### 4.3 Route 线路

字段：

-   id
-   scheme_id
-   route_name
-   route_code
-   sort_no
-   weight
-   enabled

说明：

-   不限制线路数量。
-   weight 暂保留，但标记为"待业务确认字段"。
-   默认不自动设置 20%。

### 4.4 RouteSegment 路段

字段：

-   id
-   route_id
-   segment_name
-   sort_no
-   origin_name
-   destination_name
-   distance_km
-   freight_price
-   freight_price_unit
-   load_ton
-   trips_per_vehicle_month
-   operating_months_year
-   toll_per_trip
-   loading_unloading_fee
-   information_fee
-   loaded_energy_consumption
-   empty_energy_consumption
-   electricity_price
-   enabled

V1 不限制路段数量。

### 4.5 VehiclePlan 运力/车辆方案

字段：

-   scheme_id
-   fleet_size
-   lease_type
-   down_payment_per_vehicle
-   installment_months
-   monthly_rent_per_vehicle
-   management_fee_per_vehicle
-   road_maintenance_fee
-   maintenance_fee
-   annual_inspection_fee
-   insurance_fee
-   parking_fee
-   heater_fee
-   consumable_fee
-   tire_life_km
-   tire_count
-   tire_unit_price

### 4.6 FinanceTaxPlan 财务税务方案

字段：

-   scheme_id
-   receivable_cycle
-   working_capital_loan_cycle
-   working_capital_interest_rate
-   discount_rate
-   output_vat_rate
-   input_vat_rule
-   calculation_years

税率、抵扣规则必须配置化，不允许散落在前端代码。

### 4.7 StandardParameter 标准参数库

字段：

-   id
-   parameter_code
-   parameter_name
-   category
-   value
-   unit
-   effective_date
-   expire_date
-   version
-   enabled
-   description

建议纳入：

-   管理费阶梯
-   轮胎寿命
-   轮胎数量
-   轮胎均价
-   标准满载能耗
-   标准空载能耗
-   财务利率
-   税率
-   其他公司统一测算口径

项目使用标准值后允许覆盖的，必须记录：

-   standard_value
-   override_value
-   override_reason

------------------------------------------------------------------------

## 5. 信息架构

项目详情增加一级 Tab：

`项目概览 | 项目进度 | 项目测算 | ...`

项目测算内部：

1.  测算方案
2.  参数配置
3.  测算结果
4.  现金流
5.  敏感性分析
6.  方案对比
7.  版本记录
8.  参数标准库（管理员）

------------------------------------------------------------------------

# 6. 页面 PRD

## 6.1 项目测算方案列表

### 页面目标

查看当前项目所有历史和当前测算方案。

### 顶部

展示：

-   项目名称
-   项目编号
-   客户
-   当前基准方案
-   基准方案预计利润
-   基准利润率
-   最近测算时间

### 操作

-   新建测算
-   从已有方案复制
-   进入测算
-   查看结果
-   复制方案
-   设为基准
-   归档
-   删除草稿

### 列表字段

-   方案名称
-   版本
-   状态
-   线路数
-   车辆数
-   月营收
-   月总成本
-   月利润
-   利润率
-   IRR
-   创建人
-   更新时间
-   操作

状态：

-   草稿
-   已测算
-   基准
-   已归档

------------------------------------------------------------------------

## 6.2 新建测算

采用分步式页面，不做 Excel 大表。

步骤：

`① 基础信息 → ② 线路运输 → ③ 运力成本 → ④ 财务税务 → ⑤ 确认并测算`

支持：

-   上一步
-   下一步
-   保存草稿
-   退出
-   立即测算

------------------------------------------------------------------------

## 6.3 Step 1 基础信息

字段：

-   测算方案名称\*
-   方案说明
-   租赁形式\*
-   车队规模\*
-   测算年限\*
-   预计项目开始时间
-   预计项目结束时间

租赁形式必须做枚举配置，禁止写死中文判断。

------------------------------------------------------------------------

## 6.4 Step 2 线路运输配置

页面采用：

**左侧线路列表 + 右侧线路详情**

### 线路操作

-   新增线路
-   复制线路
-   删除线路
-   调整顺序

### 线路基本信息

-   线路名称\*
-   线路说明
-   线路权重（暂作为高级字段，非必填）

### 路段配置

使用可编辑表格。

字段：

-   序号
-   路段名称
-   起点
-   终点
-   单趟里程 km\*
-   运价\*
-   运价单位\*
-   单趟载重 t\*
-   单车月趟数\*
-   年运营月数\*
-   含电损电价 元/kWh\*
-   满载能耗 kWh/100km\*
-   空载能耗 kWh/100km\*
-   单趟过路费
-   单趟装卸费
-   信息费
-   操作

操作：

-   新增路段
-   复制路段
-   删除
-   上移/下移

输入后实时展示辅助指标：

-   单车月运输量
-   单车月里程
-   路段月运输量
-   路段预计月收入
-   路段预计能源成本

辅助结果不能作为输入保存。

------------------------------------------------------------------------

## 6.5 Step 3 运力与车辆成本

分组展示。

### A. 车辆融资/租赁

-   单车首付
-   分期月份
-   单车月租

根据租赁形式动态控制字段显示和必填性。

### B. 固定运营成本

-   单车月管理费
-   路保费
-   维保费
-   年审费
-   保险费
-   停车费
-   柴暖费
-   消耗费用

每个字段展示：

`当前值 | 单位 | 来源 | 是否调整`

来源示例：

-   公司标准
-   项目填写
-   系统计算

如果覆盖公司标准值：

必须填写"调整原因"。

### C. 轮胎参数

-   轮胎寿命 km
-   单车轮胎数量
-   轮胎均价

### D. 司机成本

V1 提供"司机成本"字段，但数据模型预留 cost_type：

-   PER_VEHICLE_MONTH
-   PER_TRIP
-   FIXED_MONTH

正式生产前由业务确认默认口径。

------------------------------------------------------------------------

## 6.6 Step 4 财务与税务

### 资金参数

-   应收回款周期
-   流动资金贷款周期
-   流动资金贷款利率
-   贴现率

### 税务参数

-   销项税率
-   进项税规则
-   可抵扣成本范围

税务规则统一从 Rule Engine 获取。

------------------------------------------------------------------------

## 6.7 Step 5 确认与测算

展示参数摘要：

-   车辆数
-   线路数
-   路段数
-   平均运价
-   总预计月运输量
-   总预计月运营里程
-   关键成本参数
-   财务参数

执行前校验。

严重错误阻止测算：

-   车辆数 ≤ 0
-   路段没有里程
-   运价为空
-   载重 ≤ 0
-   月趟数 ≤ 0
-   必要能耗为空
-   电价为空
-   非法税率
-   计算分母为 0

普通警告允许继续：

-   运价明显偏离标准
-   能耗明显偏离标准
-   标准参数被覆盖
-   回款周期较长
-   某线路权重未配置

点击【开始测算】：

1.  保存当前参数快照。
2.  绑定 rule_version。
3.  后端执行计算。
4.  保存 CalculationResult。
5.  进入结果页。

------------------------------------------------------------------------

# 7. 计算引擎

## 7.1 设计原则

所有正式计算必须在后端完成。

前端可以做预览计算，但最终结果以后端为准。

计算引擎必须满足：

-   参数化
-   可测试
-   可追溯
-   可版本化
-   不依赖 UI
-   不依赖 Excel 单元格地址
-   不使用魔法数字

------------------------------------------------------------------------

## 7.2 收入模型

路段月收入：

`segment_monthly_revenue = freight_price × load_ton × trips_per_vehicle_month × fleet_size`

线路月收入：

`route_monthly_revenue = Σ segment_monthly_revenue`

项目月收入：

`project_monthly_revenue = Σ route_monthly_revenue`

注意：

如果后续确认不同路段不是独立产生完整收入，需要通过 rule_version
调整，不能直接修改历史方案。

------------------------------------------------------------------------

## 7.3 运量

路段单车月运输量：

`vehicle_monthly_volume = load_ton × trips_per_vehicle_month`

路段月运输量：

`segment_monthly_volume = load_ton × trips_per_vehicle_month × fleet_size`

------------------------------------------------------------------------

## 7.4 里程

基础：

`segment_monthly_mileage = distance_km × trips_per_vehicle_month × fleet_size`

系统必须保留：

-   单车月里程
-   线路月里程
-   项目月里程

关于"distance_km 是单程、往返还是满载段"的定义进入待确认项。

------------------------------------------------------------------------

## 7.5 能源成本

建议计算结构：

`energy_quantity = loaded_mileage × loaded_consumption / 100 + empty_mileage × empty_consumption / 100`

`energy_cost = energy_quantity × electricity_price`

由于原 Excel 的满载/空载里程划分业务含义尚未完全确认：

V1 数据模型必须分别保留：

-   loaded_distance
-   empty_distance
-   loaded_energy_consumption
-   empty_energy_consumption

若当前只输入一个 distance_km，则通过规则配置计算 loaded/empty
distance，禁止前端写死。

------------------------------------------------------------------------

## 7.6 管理费阶梯

当前 Excel 已识别规则：

-   fleet_size \< 30：2000 元/车/月
-   30 ≤ fleet_size \< 50：1500
-   50 ≤ fleet_size \< 100：1200
-   100 ≤ fleet_size \< 200：1000
-   fleet_size ≥ 200：800

实现为配置表：

ManagementFeeTier

-   min_vehicle_count
-   max_vehicle_count
-   fee
-   effective_date
-   version

禁止 if/else 硬编码在页面。

------------------------------------------------------------------------

## 7.7 轮胎成本

建议标准模型：

`cost_per_km = tire_count × tire_unit_price / tire_life_km`

`monthly_tire_cost = monthly_mileage × cost_per_km`

正式上线前与原 Excel 公式逐格核对。

------------------------------------------------------------------------

## 7.8 固定成本

至少包含：

-   车辆租金/融资成本
-   管理费
-   路保费
-   维保费
-   年审费
-   保险费
-   停车费
-   柴暖费
-   消耗费用

统一输出：

`fixed_cost_total`

每个成本项必须单独保存结果，不允许只保存总数。

------------------------------------------------------------------------

## 7.9 变动成本

至少包含：

-   司机成本
-   车辆过路费
-   装卸费
-   信息费
-   能源成本
-   轮胎成本

统一输出：

`variable_cost_total`

------------------------------------------------------------------------

## 7.10 财务成本

包含：

### 营收垫资成本

输入：

-   营收
-   回款周期
-   资金成本率

输出：

-   revenue_advance_cost

### 流动资金贷款利息

输入：

-   贷款本金/资金缺口
-   贷款周期
-   利率

输出：

-   working_capital_interest

本金计算方式当前标记为待业务确认。

------------------------------------------------------------------------

## 7.11 税务成本

至少保存：

-   output_vat
-   input_vat
-   vat_payable

基础关系：

`vat_payable = output_vat - input_vat`

但负数、留抵、不同税率、抵扣范围由税务规则配置。

------------------------------------------------------------------------

## 7.12 利润

`total_cost = fixed_cost + variable_cost + finance_cost + tax_cost`

`profit = revenue - total_cost`

`profit_margin = profit / revenue`

当 revenue = 0：

profit_margin 返回 null，不允许报除零错误。

------------------------------------------------------------------------

# 8. 现金流模型

现金流必须按月份生成，不只保存最终值。

CashFlowResult：

-   month_index
-   revenue_cash_in
-   operating_cash_out
-   vehicle_cash_out
-   financing_cash_flow
-   tax_cash_out
-   current_net_cash_flow
-   cumulative_cash_flow

计算：

`current_net_cash_flow = total_cash_in - total_cash_out`

`cumulative_cash_flow(t) = cumulative_cash_flow(t-1) + current_net_cash_flow(t)`

首付、融资、租赁的发生月份必须根据租赁规则生成。

------------------------------------------------------------------------

# 9. IRR / 投资评价

原 Excel 存在：

-   IRR 4年
-   IRR 5年
-   IRR 6年
-   IRR 8年

系统根据月度/年度现金流序列计算。

当现金流序列不满足 IRR 数学条件：

显示：

`无法计算`

并提供原因，不显示 `#NUM!`。

V1 可同时保留：

-   IRR
-   累计现金流
-   首次转正月份

后续可扩展：

-   NPV
-   投资回收期
-   盈亏平衡点

------------------------------------------------------------------------

# 10. 测算结果页

## 10.1 顶部核心指标

建议卡片：

-   月营收
-   月总成本
-   月利润
-   利润率
-   单车月收入
-   单车月利润
-   月运输量
-   月运营里程
-   IRR

------------------------------------------------------------------------

## 10.2 成本结构

展示：

-   固定成本
-   司机成本
-   能源成本
-   轮胎成本
-   过路费
-   装卸费
-   信息费
-   财务成本
-   税务成本
-   其他

提供金额和成本占比。

------------------------------------------------------------------------

## 10.3 线路经营分析

表格：

| 线路 \| 月营收 \| 固定成本 \| 变动成本 \| 财务成本 \| 税务成本 \|
  月利润 \| 利润率 \|

点击线路展开路段。

路段：

| 路段 \| 里程 \| 运价 \| 趟数 \| 运量 \| 收入 \| 能源成本 \| 其他成本
  \| 利润 \|

------------------------------------------------------------------------

## 10.4 现金流

折线/柱状展示：

-   当期现金流
-   累计现金流

下方展示月度明细表。

------------------------------------------------------------------------

# 11. 敏感性分析

## 11.1 目标

替代 Excel 右侧固定敏感性区域。

### 可选变量

V1 至少：

-   电价
-   运价
-   单车月趟数
-   单车月租
-   满载能耗

### 配置

用户选择：

-   分析变量
-   变化方式：百分比/绝对值
-   最小变化
-   最大变化
-   步长

示例：

运价：

`-10% / -5% / 基准 / +5% / +10%`

系统对每个情景重新调用完整 Calculation Engine。

结果：

-   参数变化
-   月营收
-   月成本
-   月利润
-   利润率
-   利润变化额
-   利润变化率

禁止只用简单比例推导利润。

------------------------------------------------------------------------

# 12. 方案对比

用户最多选择 3 个方案进行对比。

比较：

-   车辆规模
-   线路
-   运价
-   趟数
-   电价
-   能耗
-   月租
-   月营收
-   月成本
-   月利润
-   利润率
-   IRR
-   首次现金流转正月份

高亮"差异字段"，但不自动给出"哪个方案最好"的结论。

------------------------------------------------------------------------

# 13. 版本管理

每次正式测算保存 ParameterSnapshot。

版本字段：

-   version_no
-   parent_version_id
-   created_by
-   created_at
-   change_note
-   rule_version
-   calculation_status

参数变更记录：

-   parameter_code
-   parameter_name
-   old_value
-   new_value
-   unit
-   change_reason
-   changed_by
-   changed_at

基准版本：

-   一个项目只能存在一个当前基准版本。
-   新版本设为基准后，原基准改为历史基准。
-   历史结果不得因规则库更新自动变化。

------------------------------------------------------------------------

# 14. 参数标准库

管理员页面。

分类：

-   车辆
-   能源
-   轮胎
-   人工
-   财务
-   税务
-   管理费
-   其他

功能：

-   新增
-   编辑
-   启停
-   生效日期
-   失效日期
-   版本
-   适用范围

原则：

**历史测算绑定当时使用的参数和规则版本。**

标准库修改后，不允许静默修改历史结果。

------------------------------------------------------------------------

# 15. 计算结果可解释性

这是 V1 必须保留的能力。

用户点击某个指标，例如【能源成本】，打开"计算明细"。

示例：

能源成本\
→ 电价：0.82 元/kWh\
→ 满载能耗：135 kWh/100km\
→ 空载能耗：95 kWh/100km\
→ 月运营里程：180,000 km\
→ 计算规则：R004 / V1\
→ 结果：XXX 元

开发要求：

CalculationResultItem 至少保存：

-   result_code
-   result_name
-   result_value
-   unit
-   rule_code
-   rule_version
-   calculation_expression / explanation
-   source_parameter_snapshot

这样未来可以回答"这个数是怎么算出来的"。

------------------------------------------------------------------------

# 16. 数据库建议

核心表：

``` text
project
calculation_scheme
calculation_scheme_version
calculation_route
calculation_route_segment
vehicle_plan
finance_tax_plan
standard_parameter
standard_parameter_version
calculation_rule
calculation_rule_version
parameter_snapshot
calculation_result
calculation_result_item
cash_flow_result
sensitivity_task
sensitivity_result
parameter_change_log
```

不要按 Excel 创建：

``` text
model_sheet_1
model_sheet_2
segment_1
segment_2
...
segment_10
```

线路和路段必须是一对多动态数据结构。

------------------------------------------------------------------------

# 17. 后端服务建议

建议独立模块：

``` text
ProjectCalculationService
SchemeService
ParameterService
RuleEngine
RevenueCalculator
MileageCalculator
EnergyCostCalculator
FixedCostCalculator
VariableCostCalculator
FinanceCostCalculator
TaxCalculator
ProfitCalculator
CashFlowCalculator
InvestmentCalculator
SensitivityAnalysisService
VersionService
```

统一入口：

``` text
calculateScheme(schemeVersionId)
```

流程：

``` text
读取参数快照
→ 参数校验
→ 读取规则版本
→ 计算运营基础指标
→ 计算收入
→ 计算固定成本
→ 计算变动成本
→ 计算财务成本
→ 计算税费
→ 计算利润
→ 生成现金流
→ 计算投资指标
→ 汇总线路/项目结果
→ 保存结果明细
```

------------------------------------------------------------------------

# 18. API 建议

``` text
GET    /projects/{projectId}/calculation-schemes
POST   /projects/{projectId}/calculation-schemes
GET    /calculation-schemes/{id}
PUT    /calculation-schemes/{id}

POST   /calculation-schemes/{id}/routes
PUT    /calculation-routes/{id}
DELETE /calculation-routes/{id}

POST   /calculation-routes/{id}/segments
PUT    /calculation-segments/{id}
DELETE /calculation-segments/{id}

POST   /calculation-schemes/{id}/calculate
GET    /calculation-schemes/{id}/results
GET    /calculation-schemes/{id}/cash-flow

POST   /calculation-schemes/{id}/copy
POST   /calculation-schemes/{id}/set-baseline

POST   /calculation-schemes/{id}/sensitivity
GET    /sensitivity/{taskId}

GET    /standard-parameters
POST   /standard-parameters
PUT    /standard-parameters/{id}

GET    /calculation-schemes/{id}/versions
GET    /calculation-versions/{id}/change-log
```

------------------------------------------------------------------------

# 19. 前端交互要求

### 视觉

采用现有项目管理系统后台风格。

原则：

-   不做 Excel 风格大矩阵。
-   参数分组。
-   输入项右侧显示单位。
-   标准值使用弱提示。
-   被人工覆盖的标准参数明显标记。
-   计算结果和输入参数视觉分离。

### 保存

编辑过程中自动保存草稿。

正式点击"测算"时创建不可变参数快照。

### 数字

-   金额默认保留 2 位。
-   百分比默认保留 2 位。
-   能耗保留 2 位。
-   里程可保留 1\~2 位。
-   后端统一使用 Decimal，禁止核心财务计算使用浮点误差明显的
    Number/float。

------------------------------------------------------------------------

# 20. 校验规则

通用：

-   所有金额不得为 NaN。
-   不允许 Infinity。
-   分母为 0 时返回 null + reason。
-   百分比校验合法范围。
-   车辆数必须为正整数。
-   趟数不得为负。
-   里程不得为负。
-   载重不得为负。
-   标准参数缺失时必须明确提示。

异常格式：

``` json
{
  "code": "CALC_PARAMETER_INVALID",
  "field": "trips_per_vehicle_month",
  "message": "单车月运营趟数必须大于0"
}
```

------------------------------------------------------------------------

# 21. 权限

建议角色：

### 项目业务人员

-   新建/编辑草稿
-   执行测算
-   复制方案
-   查看结果
-   敏感性分析

### 项目负责人

以上权限 + - 设置基准方案

### 管理员/财务规则管理员

-   参数标准库
-   财税规则
-   规则版本

### 只读用户

-   查看方案及结果
-   不允许修改参数

------------------------------------------------------------------------

# 22. 审计

必须记录：

-   谁创建方案
-   谁修改参数
-   谁执行测算
-   使用哪个规则版本
-   使用哪些标准参数版本
-   谁设置基准
-   基准何时变化

------------------------------------------------------------------------

# 23. 待业务确认项

以下问题不能因为 Excel 有公式就直接视为正确业务口径：

1.  多线路"权重"的真实含义。
2.  司机成本按车/月、司机/月还是按趟。
3.  维保费按月还是按公里。
4.  路保、保险、年审在利润中如何摊销，在现金流中何时发生。
5.  营收垫资成本使用什么资金成本率。
6.  流动资金贷款本金如何计算。
7.  销项税率、进项税率及可抵扣成本范围。
8.  租赁形式具体枚举和每种形式的现金流规则。
9.  Excel 中行驶里程究竟代表单程、往返还是满载运输段。
10. 满载里程和空载里程如何划分。
11. 信息费的计费单位。
12. 敏感性分析步长使用绝对值还是百分比。
13. 多路段收入是否均独立按"运价×载重×趟数"计收。

**开发策略：这些规则必须做成可配置或隔离的 Rule 层，禁止散落硬编码。**

------------------------------------------------------------------------

# 24. Demo 验收标准

Cursor 第一阶段 Demo 至少满足：

-   [ ] 可以选择一个项目进入项目测算。
-   [ ] 可以新建测算方案。
-   [ ] 可以动态新增/删除线路。
-   [ ] 可以动态新增/删除路段。
-   [ ] 可以录入运价、载重、里程、趟数、电价、能耗等参数。
-   [ ] 可以录入车辆、固定成本、财务税务参数。
-   [ ] 点击测算后返回收入、五类成本、利润、利润率。
-   [ ] 可以查看线路和路段明细。
-   [ ] 可以查看月度现金流和累计现金流。
-   [ ] 可以执行至少5个核心参数的敏感性分析。
-   [ ] 可以复制方案。
-   [ ] 可以查看版本。
-   [ ] 可以设置基准方案。
-   [ ] 可以点击结果查看计算依据。
-   [ ] 不出现 `#DIV/0!/#NUM!/NaN/Infinity`。
-   [ ] 线路和路段数量没有 Excel 式固定上限。

------------------------------------------------------------------------

# 25. Cursor 开发要求

## 25.1 不要做的事情

1.  不要把原 Excel 直接渲染成网页。
2.  不要按单元格地址建立数据库。
3.  不要写死 5 条线路。
4.  不要写死 10 个路段。
5.  不要把所有公式写在 React/Vue 页面。
6.  不要把税率、利率、管理费阶梯写死。
7.  不要覆盖历史测算结果。
8.  不要使用前端结果作为正式财务结果。
9.  不要擅自补充原模型不存在的业务规则。

## 25.2 推荐开发顺序

``` text
1. 建数据模型
2. 建规则/参数配置
3. 实现计算引擎
4. 给计算引擎写单元测试
5. 建方案编辑页面
6. 建结果页
7. 建现金流
8. 建敏感性分析
9. 建版本管理
10. 最后再做视觉优化
```

## 25.3 测试要求

每一个 Calculator 独立单元测试。

至少覆盖：

-   正常值
-   0
-   null
-   极大值
-   边界车辆数
-   负数非法输入
-   税额为负
-   现金流无法计算 IRR
-   标准参数版本切换
-   历史方案重新查看结果不发生变化

管理费阶梯边界必须测试：

``` text
29 / 30 / 49 / 50 / 99 / 100 / 199 / 200
```

------------------------------------------------------------------------

# 26. 推荐的 Demo 首页结果布局

``` text
┌────────────────────────────────────────────────────┐
│ 项目：XXX              基准方案：V3 最终商务方案     │
├────────┬────────┬────────┬────────┬───────────────┤
│ 月营收 │ 月成本 │ 月利润 │ 利润率 │ IRR           │
├────────────────────────┬───────────────────────────┤
│ 成本结构                │ 月度现金流                │
├────────────────────────┴───────────────────────────┤
│ 线路经营明细                                        │
├────────────────────────────────────────────────────┤
│ 敏感性分析 / 方案模拟                               │
└────────────────────────────────────────────────────┘
```

------------------------------------------------------------------------

# 27. 后续 V2 预留

后续可逐步接入：

-   项目管理：自动获取项目基本资料。
-   合同/报价：自动获取线路运价。
-   TMS：实际趟次、载重、运输量、线路运营数据。
-   车联网：实际里程、满/空载能耗、电耗。
-   充电：实际电价和能源成本。
-   财务：实际回款、税费、资金成本。
-   经营分析：测算 VS 实际。
-   参数校准：利用实际项目反推公司标准参数。

最终形成：

`测算标准 → 项目立项 → 实际经营 → 偏差分析 → 标准校准 → 下一项目测算`

------------------------------------------------------------------------

# 28. 产品核心原则

这套工具不是"在线 Excel"。

它应当成为项目管理系统中的：

**标准化项目经营测算引擎。**

Excel 只是当前规则来源之一；系统的最终目标是：

**统一参数口径、统一计算规则、统一测算结果、统一版本留痕，并逐步用实际运营数据校准测算模型。**
