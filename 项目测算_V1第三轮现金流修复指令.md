# 项目测算系统 V1 第三轮现金流修复指令

## 1. 本轮目标

第二轮修改后，项目测算 V1
的运价单位、方案级年运营月数、快照冻结、司机成本、IRR
异常处理等核心能力已经基本建立。

第三轮禁止继续扩展功能。

本轮只处理现金流、投资回收期以及 VAT 现金流口径，确保：

> 月度现金流 → 年度现金流 → 累计现金流 → 首次转正月份 → IRR

使用同一套时间轴和业务规则。

优先级：

> 现金流正确性 \> 投资指标正确性 \> 测试覆盖 \> 页面展示

------------------------------------------------------------------------

# 2. P0-01 月度现金流必须计入初始投资

## 2.1 当前问题

年度现金流已经存在 Year 0：

``` text
初始车辆首付
+
首付对应进项税
```

但月度现金流当前从：

``` text
cumulative = 0
```

开始。

因此月度累计现金流没有包含项目初始投资。

这会直接影响：

-   累计现金流
-   首次现金流转正月份
-   投资回收期
-   月度现金流图

例如：

``` text
初始投资 = 1,800,000
每月净现金流 = +100,000
```

正确逻辑不可能第 1 月转正。

------------------------------------------------------------------------

## 2.2 修改原则

必须建立明确的 Month 0。

推荐：

``` text
Month 0
↓
初始投资现金流
↓
Month 1
↓
第一个经营月
↓
Month 2
...
```

Month 0 至少包含：

``` text
车辆首付现金流
首付相关税务现金流
其他明确属于项目启动期的现金流
```

不要把 Month 0 强行塞入 Month 1。

------------------------------------------------------------------------

## 2.3 累计现金流

正确：

``` text
累计现金流 Month 0
=
初始净现金流
```

``` text
累计现金流 Month N
=
Month N-1 累计现金流
+
Month N 当期净现金流
```

------------------------------------------------------------------------

## 2.4 首次转正月份

定义：

``` text
previous cumulative < 0
AND
current cumulative >= 0
```

则：

``` text
firstPositiveMonth = current monthIndex
```

如果 Month 0 已经 \>= 0：

不得错误返回 Month 1。

如果整个测算周期均未转正：

``` text
firstPositiveMonth = null
```

UI：

``` text
测算期内未转正
```

------------------------------------------------------------------------

## 2.5 必须增加独立手算测试

Case：

``` text
Month 0 = -1,800,000
Month 1~24 = +100,000/月
```

验证：

``` text
Month 17 cumulative = -100,000
Month 18 cumulative = 0
firstPositiveMonth = 18
```

这个 expected 必须写死。

禁止使用另一个 Calculator 动态生成 expected。

------------------------------------------------------------------------

# 3. P0-02 支持非整年项目经营周期

## 3.1 当前问题

项目经营期已经使用：

``` text
projectOperatingMonths
```

但年度/月度部分逻辑仍然通过：

``` text
year × 12 <= projectOperatingMonths
```

判断经营年份。

因此：

``` text
projectOperatingMonths = 30
```

可能只计算：

``` text
Month 1 ~ Month 24
```

而 Month 25 \~ Month 30 被错误截断。

------------------------------------------------------------------------

## 3.2 修改原则

项目经营周期必须以"月"为最小时间单位。

核心判断：

``` text
monthIndex <= projectOperatingMonths
```

不得先按年份判断，再反推月份。

------------------------------------------------------------------------

## 3.3 推荐架构

建议：

``` text
参数快照
↓
生成 Month 0
↓
生成 Month 1 ~ N 月度现金流
↓
按月份聚合
↓
年度现金流
↓
IRR / 累计现金流 / 投资回收期
```

尽量避免：

``` text
年度现金流自己算一套
月度现金流自己再算一套
```

否则容易产生两套口径。

------------------------------------------------------------------------

## 3.4 年运营月数仍需生效

例如：

``` text
projectOperatingMonths = 30
operatingMonthsYear = 10
```

表示项目存在 30 个月，但每个自然经营年度只有前 10 个运营月产生运输业务。

例如：

``` text
Year 1：Month 1~10 运营，11~12 非运营
Year 2：Month 13~22 运营，23~24 非运营
Year 3：Month 25~30 项目仍存在
```

Year 3 是否全部属于运营月份，应根据：

``` text
monthInYear <= operatingMonthsYear
```

判断。

------------------------------------------------------------------------

## 3.5 必测周期

增加：

``` text
12个月
18个月
24个月
30个月
36个月
42个月
54个月
60个月
```

重点验证：

``` text
18 → 不得只算12个月
30 → 不得只算24个月
42 → 不得只算36个月
54 → 不得只算48个月
```

------------------------------------------------------------------------

# 4. P0-03 修复非纯租赁尾期租金重复计提

## 4.1 当前问题

当前非纯租赁年度租金存在类似：

``` text
installmentMonths % 12
```

计算剩余月份。

如果：

``` text
installmentMonths = 30
```

则：

``` text
30 % 12 = 6
```

第 3 年计 6 个月租金是正确的。

但不能在第 4 年、第 5 年继续重复计 6 个月。

------------------------------------------------------------------------

## 4.2 正确规则

租金是否发生必须直接根据 Month Index 判断：

``` text
monthIndex <= installmentMonths
```

例如：

### 30个月

``` text
Year 1 = 12个月
Year 2 = 12个月
Year 3 = 6个月
Year 4 = 0
Year 5 = 0
```

### 36个月

``` text
12 + 12 + 12 + 0...
```

### 42个月

``` text
12 + 12 + 12 + 6 + 0...
```

### 54个月

``` text
12 + 12 + 12 + 12 + 6 + 0...
```

------------------------------------------------------------------------

## 4.3 不建议继续使用年度 residual 推导

推荐：

``` text
每个月判断是否发生租金
↓
生成月度现金流
↓
年度直接 SUM(月度租金)
```

这样自然支持任意分期月份。

------------------------------------------------------------------------

## 4.4 必须增加独立测试

至少测试：

``` text
installmentMonths = 1
6
12
18
24
30
36
42
54
60
```

其中 30 个月必须断言：

``` text
Year1 rent = monthlyRent × fleet × 12
Year2 rent = monthlyRent × fleet × 12
Year3 rent = monthlyRent × fleet × 6
Year4 rent = 0
Year5 rent = 0
```

Expected 必须手工写死。

------------------------------------------------------------------------

# 5. P1-01 VAT 留抵现金流规则

## 5.1 当前问题

利润表当前逻辑：

``` text
VAT payable
=
MAX(0, outputVAT - inputVAT)
```

但月度现金流存在：

``` text
vatCash
=
outputVAT - inputVAT
```

如果：

``` text
outputVAT = 50,000
inputVAT = 80,000
```

则：

``` text
vatCash = -30,000
```

现金流再：

``` text
-currentVAT
```

会等价于增加 30,000 元现金。

这意味着系统默认：

> 当月进项税超过销项税的部分，当月直接形成现金流入。

这个业务口径不能默认成立。

------------------------------------------------------------------------

# 6. V1 VAT 建议规则

V1 建议默认采用：

## CARRY_FORWARD

即：

``` text
本月可抵扣进项
+
上月留抵
```

用于抵扣本月销项。

------------------------------------------------------------------------

## 6.1 示例

### Month 1

``` text
销项VAT = 50,000
进项VAT = 80,000
期初留抵 = 0
```

则：

``` text
VAT现金流出 = 0
期末留抵 = 30,000
```

不是：

``` text
VAT现金流 = +30,000
```

### Month 2

``` text
销项VAT = 70,000
进项VAT = 20,000
期初留抵 = 30,000
```

可抵扣：

``` text
20,000 + 30,000 = 50,000
```

所以：

``` text
VAT现金流出 = 20,000
期末留抵 = 0
```

------------------------------------------------------------------------

# 7. VAT 数据结构建议

月度现金流建议增加或内部维护：

``` text
openingVatCredit
outputVat
inputVat
vatCreditUsed
vatCashOut
closingVatCredit
```

公式：

``` text
availableCredit
=
openingVatCredit + inputVat
```

``` text
vatCreditUsed
=
MIN(outputVat, availableCredit)
```

``` text
vatCashOut
=
MAX(0, outputVat - availableCredit)
```

``` text
closingVatCredit
=
MAX(0, availableCredit - outputVat)
```

------------------------------------------------------------------------

# 8. VAT Rule Version

当前系统已有：

``` text
negativeVatHandling
```

请真正让该配置进入现金流逻辑。

至少支持：

``` text
CARRY_FORWARD
```

如果保留：

``` text
RECOGNIZE_NEGATIVE
```

必须明确其业务含义。

禁止只是数据库有枚举，但实际 Calculator 不使用。

------------------------------------------------------------------------

# 9. 月度现金流建议统一结构

建议最终月度现金流至少包含：

``` text
monthIndex
isOperatingMonth

revenueCashIn

vehicleCashOut
operatingCashOut
financingCashFlow
taxCashOut

currentNetCashFlow
cumulativeCashFlow

openingVatCredit
outputVat
inputVat
vatCreditUsed
closingVatCredit
```

Month 0：

``` text
monthIndex = 0
```

用于初始投资。

------------------------------------------------------------------------

# 10. 年度现金流必须由月度现金流聚合

第三轮建议尽量统一为：

``` text
Monthly Cash Flow
↓
Group By Year
↓
Annual Cash Flow
```

年度现金流不要再独立复制复杂业务公式。

例如：

``` text
Year 1
=
SUM(Month 1 ~ Month 12)
```

``` text
Year 2
=
SUM(Month 13 ~ Month 24)
```

30个月项目：

``` text
Year 3
=
SUM(Month 25 ~ Month 30)
```

而不是把 Year 3 直接判定无效。

------------------------------------------------------------------------

# 11. IRR 口径

IRR 必须使用包含初始投资的现金流序列。

例如年度：

``` text
Year 0
Year 1
Year 2
Year 3
...
```

月度如未来计算 Monthly IRR：

``` text
Month 0
Month 1
Month 2
...
```

禁止：

``` text
从 Month 1 开始计算 IRR
```

------------------------------------------------------------------------

# 12. 投资回收期口径

V1 当前核心展示"首次现金流转正月份"。

定义统一为：

``` text
首次 cumulativeCashFlow >= 0 的经营月份
```

且必须考虑 Month 0 初始投资。

例如：

``` text
Month0 = -100万
Month1累计 = -90万
...
Month10累计 = 0
```

则：

``` text
firstPositiveMonth = 10
```

如果测算期结束：

``` text
cumulativeCashFlow < 0
```

则：

``` text
firstPositiveMonth = null
```

显示：

``` text
测算期内未转正
```

------------------------------------------------------------------------

# 13. 必须增加的第三轮 Golden Cases

本轮测试不能只做：

``` text
calculateA()
vs
calculateB()
```

如果 A/B 最终共享同一套实现，这种测试不能证明业务公式正确。

必须增加手工 expected。

------------------------------------------------------------------------

## Golden CashFlow 01：初始投资

输入：

``` text
Initial = -1,800,000
Monthly Net = +100,000
```

Expected：

``` text
Month0 = -1,800,000
Month17 cumulative = -100,000
Month18 cumulative = 0
firstPositiveMonth = 18
```

------------------------------------------------------------------------

## Golden CashFlow 02：30个月经营期

``` text
projectOperatingMonths = 30
```

Expected：

``` text
Month1~30存在
Month31停止经营
Year3包含Month25~30
```

------------------------------------------------------------------------

## Golden CashFlow 03：18个月经营期

Expected：

``` text
Year1 = Month1~12
Year2 = Month13~18
```

------------------------------------------------------------------------

## Golden CashFlow 04：30个月分期

Expected：

``` text
Year1 = 12个月租金
Year2 = 12个月租金
Year3 = 6个月租金
Year4 = 0
```

------------------------------------------------------------------------

## Golden CashFlow 05：42个月分期

Expected：

``` text
12 + 12 + 12 + 6
```

后续：

``` text
0
```

------------------------------------------------------------------------

## Golden CashFlow 06：VAT 留抵

Month1：

``` text
Output = 50,000
Input = 80,000
```

Expected：

``` text
vatCashOut = 0
closingCredit = 30,000
```

Month2：

``` text
Output = 70,000
Input = 20,000
OpeningCredit = 30,000
```

Expected：

``` text
vatCashOut = 20,000
closingCredit = 0
```

------------------------------------------------------------------------

# 14. 回归测试

第三轮修改不得破坏第二轮已经通过的能力。

必须重新执行：

-   Excel 示例 AC39--AC62
-   元/吨
-   元/趟
-   元/吨公里
-   空载
-   管理费边界
-   多路段成本分摊
-   司机成本覆盖
-   利润率除零
-   IRR 异常
-   敏感性 0%
-   Snapshot 不可变

尤其：

> 修现金流时禁止修改原 Excel
> 月度利润表口径，除非有明确测试证明原公式错误。

------------------------------------------------------------------------

# 15. 页面回归

检查：

## 现金流页面

必须正确显示：

``` text
初始投资
月度净现金流
累计现金流
首次转正月份
```

如果图表不展示 Month 0，也必须保证累计现金流已经包含 Month 0。

更推荐展示：

``` text
0期 / 初始投入
```

------------------------------------------------------------------------

# 16. 本轮禁止修改

禁止：

-   AI
-   OCR
-   尽调助手
-   自动补参
-   AI风险评估
-   新增大屏
-   重做项目管理
-   重做方案页面
-   新增审批
-   大规模 UI 重构
-   修改无关数据库结构
-   擅自修改 Excel 已确认利润表公式

只处理：

> 现金流 + 项目周期 + 租金周期 + VAT留抵 + 对应测试。

------------------------------------------------------------------------

# 17. 完成后输出第三轮验收报告

生成：

``` text
docs/V1_THIRD_ROUND_ACCEPTANCE.md
```

至少包含：

  ID      问题                   状态        修改文件   测试Case   说明
  ------- ---------------------- ----------- ---------- ---------- ------
  P0-01   初始投资进入月现金流   PASS/FAIL                         
  P0-02   非整年项目周期         PASS/FAIL                         
  P0-03   尾期租金               PASS/FAIL                         
  P1-01   VAT留抵                PASS/FAIL                         

------------------------------------------------------------------------

# 18. 自动化测试报告

必须记录真实执行结果：

``` text
Test Suites:
Tests:
Passed:
Failed:
Duration:
```

同时执行：

``` text
npm test
```

``` text
type check
```

``` text
lint
```

``` text
build
```

任何一项失败，不得写"全部通过"。

------------------------------------------------------------------------

# 19. Definition of Done

第三轮只有同时满足以下条件才算完成：

-   Month 0 初始投资进入现金流
-   累计现金流包含初始投资
-   首次转正月份计算正确
-   18/30/42/54 月项目周期正确
-   30/42/54 月分期租金不重复
-   VAT CARRY_FORWARD 正确
-   VAT 负值不默认形成现金流入
-   年度/月度现金流口径一致
-   IRR 包含初始投资
-   独立手算 Golden Case 通过
-   第二轮 Excel Golden 回归通过
-   Snapshot 回归通过
-   Sensitivity 回归通过
-   Type Check 通过
-   Test 通过
-   Build 通过
-   输出 `V1_THIRD_ROUND_ACCEPTANCE.md`

完成后停止继续开发，等待产品第三轮验收。
