# 项目测算系统——终审修改优化清单（Cursor 执行版）V1.0

> 适用基线：当前 Cursor 重构提交 `bfbd19aa98ee5852bbafeef27c77497a4a5d05dc`
>
> 本轮目标：**不重构 Calculation Engine、不新增业务计算口径、不继续扩功能，只完成领导演示前的交互可靠性与体验收口。**

---

## 1. 本轮改造原则

1. **Calculation Engine 为唯一计算真源**：不得在页面层重新实现正式财务/运营计算公式。
2. **UI 可以实时预览，但正式结果必须来自现有计算引擎。**
3. **不得为了还原效果图擅自新增模型不存在的参数**，例如装卸时间反推趟次、运营效率反推车辆需求等。
4. 本轮优先解决：
   - 用户能否顺利完成五步测算；
   - 输入是否可靠保存；
   - 校验是否及时、清晰；
   - 结果是否适合领导快速阅读；
   - 方案 A/B 是否容易理解和演示。
5. 不在本轮继续增加新的一级模块。

---

# 2. P0——演示前必须修复

## P0-01 五步向导增加“逐步校验”，禁止空数据一路下一步

### 当前问题

当前“下一步”主要执行保存并切换步骤，没有针对当前步骤进行前置必填校验。

这会导致用户：

`基本信息没填完整 → 运输场景没填 → 运营参数没填 → 一直进入第5步 → 最后一次性看到大量错误`

这不符合向导式产品的预期。

### 改造目标

每一步只负责校验当前步骤必须完成的内容。

点击“下一步”时：

`flush当前输入 → 保存成功 → 校验当前步骤 → 无阻断错误 → 进入下一步`

存在错误则停留当前页，并将错误定位到具体字段。

### 建议校验范围

#### Step 1 基本信息

至少校验：

- 项目名称
- 客户名称
- 项目经理
- 测算方案名称
- 租赁形式
- 测算年限 > 0
- 年运营月数 1~12

#### Step 2 运输场景

至少校验：

- 至少存在 1 条线路
- 每条有效线路至少存在 1 个路段
- 装货地
- 卸货地
- 单程距离 > 0
- 单趟载重 > 0

#### Step 3 运营参数

至少校验：

- 车队规模 > 0
- 单车月趟数 > 0
- 年运营月数合法

#### Step 4 成本收益

根据当前 Calculation Engine / Rule Engine 的正式要求校验：

- 运价及运价单位
- 电价
- 满载能耗
- 当前租赁形式要求的必填车辆成本字段
- 公司标准参数被覆盖时必须填写调整原因
- 其他会阻断 Calculation Engine 的必填项

### 交互要求

错误不要只在顶部 Banner 展示。

必须同时：

- 字段红色边框；
- 字段下方显示具体错误原因；
- 页面顶部显示“当前还有 X 项需要完成”；
- 点击顶部错误可滚动定位对应字段；
- 第一个错误自动滚动进入视口，但不要强制抢焦点导致输入中断。

### 验收标准

- Step 2 缺卸货地时无法进入 Step 3。
- 填写完成后无需刷新即可继续。
- 错误信息明确指出哪个字段有问题。
- 不允许出现“完整度很高但不知道哪里不能继续”的情况。

---

## P0-02 重构自动保存机制，解决 Debounce 与正式测算的数据竞争

### 当前问题

当前存在 800ms debounce 自动保存，同时“下一步 / AI检查 / 开始测算”又会主动调用 persist。

用户可能出现：

`刚修改参数 → 立即点击开始测算 → UI 已是新值 → 某些 pending segment 修改仍未落库 → 后端使用旧值测算`

这是演示和正式系统都不能接受的问题。

### 改造目标

建立统一的：

`flushPendingChanges()`

所有关键操作必须先调用它。

### 必须覆盖的 pending 内容

- Scheme 基础参数 debounce；
- Vehicle 参数；
- Finance 参数；
- Segment 参数 debounce；
- 路线名称/说明；
- 公司标准覆盖原因；
- 项目信息修改。

### 建议调用链

#### 点击“下一步”

`await flushPendingChanges()`

→ `await validateCurrentStep()`

→ `setStep(step + 1)`

#### 点击“AI检查”

`await flushPendingChanges()`

→ `await validate()`

→ AI/规则解释

#### 点击“开始测算”

`await flushPendingChanges()`

→ `await validate()`

→ errors = 0

→ POST calculate

→ results

### 保存状态

顶部保存状态统一为：

- 正在保存…
- 已保存
- 保存失败

当处于“正在保存”时，正式测算按钮可显示 Loading，并等待保存完成，而不是直接继续。

### 验收标准

测试场景：

1. 修改单车月趟数；
2. 100ms 内立即点击开始测算；
3. 正式结果必须使用刚刚修改的新值。

同样测试：运价、电价、能耗、载重、距离、车队规模。

---

## P0-03 路线名称/说明禁止每输入一个字符就 PUT + refreshRoutes

### 当前问题

路线名称和线路说明当前在 `onChange` 中直接：

`PUT → refreshRoutes()`

输入一个名称可能触发多次网络请求和整棵数据刷新。

风险：

- 输入卡顿；
- 中文输入法体验异常；
- 光标跳动；
- 请求竞争；
- 不必要的后端压力。

### 修改方案

页面输入时只更新 local state。

采用以下任一方式保存：

- 推荐：500~800ms debounce；
- 或：onBlur 保存。

不要在每个字符保存后执行 `refreshRoutes()`。

保存成功后只更新本地 route state；除非后端确实产生结构变化，否则不要重新 GET 全方案。

### 验收标准

连续输入：

`云南玉溪钢材运输线路`

输入过程必须顺滑，不能出现明显刷新、卡顿、字符丢失或光标跳动。

---

# 3. P1——演示前强烈建议修复

## P1-01 “完整度”与正式校验规则统一

### 当前问题

当前 `calcCompleteness()` 是独立的一套字段检查，而正式测算使用 Rule Engine / calculate validate。

因此可能出现：

`完整度 100% → 点击开始测算 → 仍然存在阻断错误`

### 改造目标

完整度不应自行定义另一套业务真相。

建议：

- 页面完整度用于表达“填写进度”；
- 正式可测算状态由 Rule Engine 决定；
- 两者明确区分。

### 推荐 UI

顶部：

`填写进度 92% · 待完成 2 项`

第 5 步：

`测算校验：2 项阻断 / 3 项提醒`

不要把“填写完整度”直接等价于“可以测算”。

---

## P1-02 重命名第 3 步，避免制造不存在的计算逻辑

### 当前问题

当前文案：

> 一天为什么能跑这些趟

但模型实际输入的是：

> 单车月趟数

系统并没有通过装货时间、卸货时间、在途时间等自动推导日趟次。

### 修改建议

Step 3 名称建议改为：

**运营效率**

页面主标题：

**车辆运营与运输效率**

说明：

> 当前测算模型以“单车月趟数”作为核心运营输入。请根据历史运营数据、现场调研或项目假设填写，系统将据此计算运输能力、里程、收入及相关成本。

实时反馈继续保留：

- 单车月运量；
- 单车月里程；
- 单车年运输能力；
- 路段月运量。

如果希望更业务化，可额外展示一个**仅展示、不进入正式模型的新计算口径不得新增**。不要自行反推日趟数。

---

## P1-03 重构结果页顶部 KPI 层级

### 当前问题

结果页目前第一组核心指标仍偏月度财务分析：

- 月营收
- 月总成本
- 月利润
- 利润率
- IRR
- 单车月收入
- ……

对于领导演示，第一眼信息密度太高且偏专业。

### 改造目标

顶部第一屏优先回答：

1. 一年能运多少货？
2. 一年收入多少？
3. 一年成本多少？
4. 一年利润多少？
5. 利润率多少？
6. 多久回本？

### 推荐顶部 KPI

第一排：

- 年运输量
- 年收入
- 年总成本
- 年利润
- 利润率
- 投资回收期

如页面宽度不足，可 3×2。

第二层“经营效率”再展示：

- 月运输量
- 月运营里程
- 单车月收入
- 单车月利润
- IRR

### 注意

年数据继续严格使用：

`月度引擎结果 × operatingMonthsYear`

不得自行引入新的年化口径。

---

## P1-04 “AI 测算摘要”与固定规则摘要区分

### 当前问题

当前结果页的“AI 测算摘要”实际上由前端固定模板拼接引擎结果产生，并非真正的大模型生成。

演示时容易被追问：

> 这个 AI 真的是模型分析的吗？

### 修改方案

如果当前没有实际调用大模型：

将标题改为：

**智能测算摘要**

或：

**系统测算摘要**

并保留：

> 所有数字来自确定性计算引擎。

真正接入模型后，再使用“AI分析”。

### AI 检查同理

如果当前仅调用规则引擎，按钮建议：

**智能检查参数**

执行后可显示：

- 规则校验结果；
- 如果大模型可用，再增加“AI解释”。

不要用固定模板假装大模型调用成功。

---

## P1-05 对比方案增加“来源方案 + 修改项”提示

### 当前问题

从结果页点击“创建对比方案”后直接复制并进入新方案，用户不容易感知：

- 新方案从谁复制；
- 它和基准方案是什么关系；
- 自己到底改了什么。

### 修改目标

复制成功后，新方案顶部显示：

> 方案 B
>
> 基于：基准方案 A
>
> 当前已调整 0 项参数

修改参数后实时更新：

> 当前已调整 4 项参数

支持点击“查看差异”，展示相对于来源方案变化的参数。

### 方案对比页

继续保留：

- 结果差异；
- 参数差异；
- 只看差异参数。

建议增加：

**主要差异来源**

例如：

> 两方案存在 4 项输入参数差异：车队规模、单车月趟数、电价、运价。

只能描述事实，不要擅自输出“推荐方案”。

---

# 4. P2——体验优化，可在 P0/P1 完成后处理

## P2-01 普通模式减少专业术语暴露

普通模式目标用户是业务人员，不是财务模型负责人。

建议对以下字段增加解释：

- 租赁形式；
- 测算年限；
- 年运营月数；
- 应收回款周期；
- 流动资金贷款周期；
- 折现率；
- IRR；
- 首次现金流转正。

普通模式优先显示业务语言，专业模式显示计算口径和公式。

---

## P2-02 方案对比增强“决策阅读感”，但不要做自动推荐

当前表格功能正确，可增加顶部摘要：

- 参与对比方案数量；
- 有差异的核心指标数量；
- 有差异的输入参数数量；
- 最大金额变化项；
- 主要参数差异。

不要自动输出：

> 方案 B 最优 / 建议选择方案 B

只展示客观差异，让业务人员自己判断。

---

## P2-03 第 5 步“测算前检查”增强可操作性

第 5 步不要只展示问题数量。

建议分为：

### 必须处理

红色区域：阻断测算。

每条包含：

- 问题字段；
- 当前值；
- 原因；
- “去修改”按钮。

### 风险提醒

黄色区域：允许继续。

### 已通过

默认折叠，只显示数量。

例如：

`✓ 已通过 28 项`

---

# 5. 五步向导最终推荐结构

## Step 1 项目与方案

用户回答：

> 我在测什么项目？

内容：

- 项目名称；
- 客户；
- 项目经理；
- 方案名称；
- 租赁形式；
- 测算年限；
- 年运营月数；
- 开始/结束时间；
- 项目描述。

---

## Step 2 运输场景

用户回答：

> 货从哪里运到哪里，一趟运多少？

内容：

- 线路；
- 路段；
- 装货地；
- 卸货地；
- 距离；
- 单趟载重。

线路/路段的数据结构保留，但文案尽量业务化。

---

## Step 3 运营效率

用户回答：

> 这些车一个月能跑多少业务？

内容：

- 车队规模；
- 单车月趟数；
- 年运营月数；
- 实时产能反馈。

---

## Step 4 收入与成本

用户回答：

> 项目怎么收费、钱花在哪里？

结构：

1. 运输收入；
2. 车辆成本；
3. 能源成本；
4. 人员成本；
5. 维修/运营成本；
6. 保险税费；
7. 其他成本；
8. 高级财务税务参数。

普通模式折叠高级参数。

---

## Step 5 测算检查

用户回答：

> 这些参数现在能不能正式测算？

展示：

- 填写进度；
- 阻断问题；
- 风险提醒；
- 公司标准覆盖项；
- 已通过项；
- 开始测算。

---

# 6. 结果页最终信息架构

## 第一屏——决策结果

### 核心 KPI

- 年运输量；
- 年收入；
- 年总成本；
- 年利润；
- 利润率；
- 投资回收期。

### 系统测算摘要

使用引擎真实结果拼装，不能生成不存在的数字。

---

## 第二屏——经营效率

- 月运输量；
- 月运营里程；
- 单车月收入；
- 单车月利润；
- IRR；
- IRR 4/5/6/8 年。

---

## 第三屏——成本结构

- 车辆；
- 能源；
- 司机；
- 固定运营；
- 轮胎；
- 路桥；
- 装卸；
- 信息；
- 财务；
- 税务。

---

## 第四屏——线路经营分析

保留现有下钻能力。

---

## 第五屏——专业明细

提供：

- 查看完整测算明细；
- 计算表达式；
- 来源参数快照；
- Rule Code / Version；
- Snapshot ID。

不要把这些内容放在领导第一屏。

---

# 7. 本轮明确禁止修改的内容

Cursor 本轮不得：

1. 重写 Calculation Engine；
2. 修改已有公式口径；
3. 为 UI 效果新增虚构计算公式；
4. 用前端估算结果替代正式结果；
5. 修改 Rule Pack 业务含义，除非是明确 Bug；
6. 删除 Snapshot 机制；
7. 删除计算表达式与来源参数追溯能力；
8. 因 UI 重构改变现有 API 的业务语义；
9. 擅自新增数据库字段，除非 P0/P1 无法在现有结构完成，并需先说明原因；
10. 继续增加新的一级功能模块。

---

# 8. Cursor 实施顺序

严格按以下顺序执行：

### 第一阶段：可靠性

1. 实现统一 `flushPendingChanges()`；
2. 修复 segment/scheme 保存竞争；
3. 修复路线名称逐字请求；
4. 实现逐步校验。

完成后先运行测试。

### 第二阶段：演示体验

5. 调整 Step 3 文案与信息层级；
6. 重构 Step 5 检查页面；
7. 重构结果页顶部 KPI；
8. 修正 AI / 系统摘要命名；
9. 增加方案复制来源提示。

### 第三阶段：体验优化

10. 普通模式术语解释；
11. 方案对比摘要；
12. 错误定位和滚动体验。

---

# 9. 必须执行的回归测试

## 9.1 保存可靠性

分别修改以下参数后，在 100ms 内立即点击“下一步/开始测算”：

- 车队规模；
- 距离；
- 载重；
- 单车月趟数；
- 运价；
- 电价；
- 满载能耗；
- 空载能耗；
- 司机成本；
- 车辆成本。

结果必须使用最新值。

---

## 9.2 五步校验

测试空白项目：

- Step 1 缺必填项不能下一步；
- Step 2 缺卸货地不能下一步；
- Step 2 距离为 0 不能下一步；
- Step 3 趟数为 0 不能下一步；
- Step 4 缺引擎要求参数不能进入正式测算；
- 修改后错误应立即消失。

---

## 9.3 正式测算一致性

对同一组参数：

- UI 实时预览；
- 正式 Calculation Engine；
- 结果页；
- 方案对比页；

涉及同一指标时必须保持口径一致。

---

## 9.4 Snapshot

正式测算后：

1. 保存结果 A；
2. 修改参数；
3. 再次测算得到结果 B；
4. 打开 A 的 snapshot；

A 必须保持原结果，不得被 B 覆盖。

---

## 9.5 方案对比

流程：

`基准方案 A → 创建对比方案 B → 修改 B 的运价/电价/趟数 → B 测算 → A/B 对比`

必须确认：

- A 不被修改；
- B 来源清晰；
- 参数差异正确；
- 结果差异正确；
- 金额差值方向正确；
- 不自动输出“哪个方案最好”。

---

# 10. 最终领导演示链路验收

最终必须完整人工跑通：

`工作台`

→ `新建/进入项目`

→ `Step 1 项目与方案`

→ `Step 2 运输场景`

→ `Step 3 运营效率`

→ `Step 4 收入与成本`

→ `Step 5 测算检查`

→ `开始测算`

→ `结果决策页`

→ `创建方案 B`

→ `修改关键参数`

→ `重新测算`

→ `A/B 方案对比`

### 演示通过标准

- 无阻断报错；
- 无明显页面卡顿；
- 中文输入正常；
- 不出现旧参数参与测算；
- 无 NaN / Infinity / #DIV/0；
- 所有正式数字来自 Calculation Engine；
- 年/月口径说明明确；
- AI 与规则能力不混淆；
- 方案 A 不会被 B 覆盖；
- 结果页第一屏领导可以在 10 秒内看懂项目核心测算结论。

---

# 11. Cursor 完成后必须输出的交付报告

修改完成后不要只回复“已完成”。

必须输出：

## A. 修改文件清单

列出所有新增/修改文件。

## B. P0/P1/P2 完成状态

逐条说明：

- 已完成；
- 部分完成；
- 未完成；
- 原因。

## C. Calculation Engine 影响说明

明确说明：

- 是否修改计算引擎；
- 是否修改公式；
- 是否修改 Rule Pack；
- 是否修改数据库结构；
- 是否修改 API 业务语义。

如果有，必须逐项列出。

## D. 测试报告

至少执行：

- TypeScript / lint；
- Unit Test；
- Build；
- 核心 E2E；
- 五步完整人工/自动化流程；
- 保存竞争测试；
- Snapshot 测试；
- 方案对比测试。

## E. Git 提交

全部通过后统一提交 Git，并给出 Commit SHA。

---

# 12. 可直接复制给 Cursor 的执行指令

请基于当前最新代码执行《项目测算系统——终审修改优化清单（Cursor 执行版）V1.0》。

本轮不是继续扩功能，而是领导演示前最后一次 UX 与可靠性收口。

硬性要求：

1. 不重写 Calculation Engine；
2. 不改变已验证计算公式和业务口径；
3. 不为了 UI 新增虚构计算参数；
4. 优先完成 P0，再完成 P1，最后处理 P2；
5. 必须解决 debounce 自动保存与正式测算之间的数据竞争；
6. 五步向导必须逐步校验，不能允许用户带着当前步骤的阻断缺失直接进入下一步；
7. 路线名称等文本输入禁止每个字符 PUT + refresh；
8. 结果页第一屏改为领导决策视角，优先展示年运输量、年收入、年成本、年利润、利润率、投资回收期；
9. 固定模板摘要不得称为真正的 AI 分析；
10. 创建方案 B 后必须能识别其来源方案及修改差异；
11. 修改完成后执行完整回归测试，不允许只验证 Build；
12. 最终输出修改文件、P0/P1/P2 状态、Calculation Engine 影响、测试报告、Commit SHA。

如果现有实现与本清单存在冲突，以“Calculation Engine 口径不变、数据保存可靠、业务人员操作简单”为最高优先级。
