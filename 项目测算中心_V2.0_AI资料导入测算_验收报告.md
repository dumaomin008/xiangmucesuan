# 项目测算中心 V2.0 AI 资料导入测算——验收报告

> 基于基线 Commit `3a96541` 增量交付。
> 报告生成时间：2026-09-18

## 1. Commit SHA

`989403a321f00b87accfae3576e4e996ac9ce34d`（基线 `3a96541`）

## 2. 修改文件

| 文件 | 原因 |
|------|------|
| `src/demo/import/*` | ImportSession、DocumentParserAdapter、mapToInput、补参、Tool |
| `src/demo/repository/importRepository.ts` | ImportSession LocalStorage |
| `src/demo/bootstrap.ts` / `keys.ts` / `index.ts` / `browser-bridge.ts` | 接入 importApi |
| `demo-frontend-package/import-app.js` | 测算中心三入口、导入页、参数确认 |
| `demo-frontend-package/calculation-app.js` | 首页重构 + 路由 |
| `demo-frontend-package/index.html` / `styles.css` | 入口与样式 |
| `demo-frontend-package/lib/pm-calc.bundle.js(.map)` | 浏览器包同步 |
| `src/demo/__tests__/ai-import.test.ts` | 导入单测 |
| `src/demo/__tests__/phase3-fusion.test.ts` | 无前端 Key 断言补强 |
| `tests/demo-e2e/final-demo.spec.ts` | V2 导入 E2E |

**未修改** Calculation Engine 公式、Excel 对齐口径、结果页结构。

## 3. 页面路由

| 路由 | 用途 |
|------|------|
| `/calculation` | 测算中心首页（三入口） |
| `/calculation/import` | AI 资料上传 |
| `/calculation/import/:id` | 参数确认 / 补参 / 开始测算 |
| `/calculation/link` | 关联已有项目 |
| `/calculation/manual` | 手动创建（进入已有参数页） |

## 4. 新数据模型

`ImportSession` + `ImportFile` + `ExtractedParameter`。

参数状态：`EXTRACTED | MISSING | CONFLICT | INFERRED | MANUAL | CONFIRMED`。

## 5. DocumentParserAdapter

- 接口与 UI 解耦，模式 `demo | live`。
- Demo 模式：按文件名返回稳定结构化参数（含冲突与推断），不在 UI 写死数字。
- Live 模式：未接真实模型时返回失败，前端可重试，不伪造 KPI。

## 6. AI Tool 清单（导入）

| Tool | 写数据 | 调引擎 |
|------|--------|--------|
| createImportSession / addImportFiles / loadDemoSampleFiles | 会话 | 否 |
| parseImportFiles / retryImportFile | 会话参数 | 否 |
| resolveConflict / confirmInferred / confirmExtracted | 会话参数 | 否 |
| previewImportParamPatches / applyImportParamPatches | 确认后写会话 | 否 |
| getImportSummary / canStartCalculation | 否 | 否 |
| createScenarioFromImport | **Scenario** | **是** |

AI 不直接写 Scenario，不生成 KPI。未确认（冲突/推断/缺失）不可开始测算。

## 7. 参数映射

导入字段经 `mapToCalculationInput` 写入既有 `SchemeCalculationInput`（车辆数、月租、路段电价/运价/里程/载重/能耗/趟次、司机成本等），再走现有 `calculateProject`。

## 8. 文件类型与能力边界

| 类型 | Demo | 真实解析 |
|------|------|----------|
| PDF / Excel / Word / 图片 | 按演示样本走 Adapter | 未接入，失败可重试 |
| API Key | 不进前端 | 仅服务端（本轮 Demo 不调用） |

## 9. 测试结果

| 套件 | 结果 |
|------|------|
| `src/demo/__tests__/ai-import.test.ts` | **6 passed** |
| `src/lib/engine` + `src/demo/__tests__` | **158 passed**（含 Excel 对齐 19） |
| Playwright `playwright.demo.config.ts` | **5 passed**（含 V2 AI 导入） |

## 10. 是否修改 Calculation Engine

否。

## 11. 已知问题

- Demo 解析是稳定样本，不是真实 OCR/表格抽取。
- 同路由刷新依赖 `render()`（`go()` 相同 hash 不触发 `hashchange`），已在导入页处理。

## 12. 下一阶段生产化

接入真实 DocumentParser（PDF/Excel），服务端异步解析，人工确认流保持不变，再调用同一 Calculation Engine。
