# 项目测算计算引擎规则说明 V1.0

> 配套文件：`项目测算工具_PRD_V1.0_Cursor开发版.md`\
> 规则来源：`重卡运力测算模型-V5(1).xlsx` 中 `测算模型(1)`
> 的公式逆向分析。\
> 目标：让 Cursor 先按原 Excel
> 口径实现一套可运行、可测试、可追溯的计算引擎，再将待确认业务口径逐步替换为正式规则。
>
> **重要原则：原 Excel 是当前"参考实现"，不是永远正确的业务真理。**
> 本文分为【已确认原公式】、【产品化解释】和【待业务确认】三层。开发不得自行猜测待确认项。

------------------------------------------------------------------------

## 1. 计算层级

原 Excel 实际混合了三个计算层：

1.  **路段月度经济性**：每个路段分别计算收入及分摊成本。
2.  **线路月度经济性**：汇总该线路全部路段。
3.  **项目长期投资测算**：按年生成收入、成本、现金流并计算 IRR。

系统必须拆成独立计算阶段：

``` text
输入参数
→ 路段运营量
→ 线路收入
→ 线路固定成本
→ 线路变动成本
→ 财务成本
→ 税务成本
→ 月利润
→ 年度现金流
→ 累计现金流
→ IRR
→ 敏感性分析
```

------------------------------------------------------------------------

# 2. 原 Excel 输入参数映射

  Excel     字段                 系统字段建议                        层级
  --------- -------------------- ----------------------------------- ------
  E3        租赁形式             lease_type                          方案
  E4        车队规模             fleet_size                          方案
  E5:N5     含电损均价           electricity_price                   路段
  E6:N6     行驶里程             distance_km                         路段
  E7:N7     去程运价             freight_price                       路段
  E8:N8     载重                 load_ton / load_flag                路段
  E9:N9     单车月运营趟数       trips_per_vehicle_month             路段
  E10:N10   年运营月数           operating_months_year               路段
  E11       单车首付             down_payment_per_vehicle            方案
  E12       单车分期月份         installment_months                  方案
  E13       月租                 monthly_rent_per_vehicle            方案
  E14       单车月管理费         management_fee_per_vehicle          方案
  E15       单车月路保费         road_maintenance_fee                方案
  E16       单车月维保费         maintenance_fee                     方案
  E17       单车审验费用/月      inspection_fee_monthly              方案
  E18       单车月保险费         insurance_fee_monthly               方案
  E19       单车月停车费         parking_fee_monthly                 方案
  E20       单车月柴暖费         heater_fee_monthly                  方案
  E21       单车消耗费用         consumable_fee_monthly              方案
  E22:N22   单车司机成本/趟      driver_cost_per_trip                路段
  E23:N23   单车过路费/趟        toll_per_trip                       路段
  E24:N24   单车单趟装卸费       loading_fee_per_trip                路段
  E25:N25   单车单趟信息费       information_fee_per_trip            路段
  E26       应收回款周期         receivable_cycle_months             财务
  E27       流动资金贷款周期     working_capital_loan_cycle_months   财务
  E28       车辆折旧年限         depreciation_months                 财务
  E29       轮胎寿命             tire_life_km                        标准
  E30       单车轮胎数量         tire_count                          车辆
  E31       轮胎均价             tire_unit_price                     标准
  E32       满载平均能耗         loaded_consumption_kwh_km           能源
  E33       空载平均能耗         empty_consumption_kwh_km            能源
  E34       营收年贴现率         revenue_discount_rate               财务
  E35       流动资金贷款年利率   working_capital_interest_rate       财务

### 关键发现

原表 E32/E33 的单位是 **kWh/km**，不是 kWh/100km。系统字段和 UI
必须统一单位，避免扩大/缩小 100 倍。

------------------------------------------------------------------------

# 3. 收入模型

## R-REV-001 路段月收入

原 Excel：

``` text
车辆数 × 运价 × 载重 × 单车月趟数
```

公式示例：

``` excel
=$E$4*$E$7*$E$8*$E$9
```

系统：

``` text
segmentMonthlyRevenue =
fleetSize
× freightPrice
× loadTon
× tripsPerVehicleMonth
```

线路月收入：

``` text
routeMonthlyRevenue = Σ segmentMonthlyRevenue
```

原 Excel 汇总公式：

``` excel
=车队规模 × SUMPRODUCT(运价范围, 载重范围, 趟数范围)
```

### 注意

原表字段名称写"去程运价（吨/元）"，但公式表现为 `元/吨 × 吨`。UI
建议显示为 **运价（元/吨）**，正式上线前业务确认。

------------------------------------------------------------------------

# 4. 固定成本模型

原 Excel 的固定成本并不是直接平均分给每个路段，而是：

1.  先计算线路级固定成本；
2.  再按照 **路段里程 × 路段趟数**
    占线路总"里程×趟数"的比例分摊到各路段。

分摊权重：

``` text
segmentAllocationWeight =
(segmentDistance × segmentTrips)
/
Σ(allSegmentDistance × allSegmentTrips)
```

因此系统应保留：

``` text
routeFixedCost
segmentAllocatedFixedCost
allocationWeight
```

## R-FIX-001 车辆成本/月

原 Excel 月度线路口径：

### 纯租赁

``` text
vehicleCost =
fleetSize × monthlyRent × 12 / operatingMonths
```

### 非纯租赁

``` text
vehicleCost =
fleetSize
× (downPayment / depreciationMonths + monthlyRent)
× 12 / operatingMonths
```

这里原表把首付按折旧月份摊入经营成本。

**待确认：**这是会计/经营测算口径，不代表真实现金流口径。

## R-FIX-002 管理费

原 Excel：

``` text
fleetSize < 30       → 2000 元/车/月
30 ≤ fleetSize < 50  → 1500
50 ≤ fleetSize < 100 → 1200
100 ≤ fleetSize <200 → 1000
fleetSize ≥ 200      → 800
```

线路成本：

``` text
fleetSize × managementFee × 12 / operatingMonths
```

必须配置化。

## R-FIX-003 \~ R-FIX-009

路保、维保、审验、保险、停车、柴暖、消耗费用统一采用：

``` text
annualizedCost =
fleetSize × monthlyCost × 12 / operatingMonths
```

随后按路段 `里程×趟数` 权重分摊。

------------------------------------------------------------------------

# 5. 变动成本

## R-VAR-001 司机成本

原 Excel：

``` text
segmentDriverCost =
fleetSize × trips × driverCostPerTrip
```

线路：

``` text
routeDriverCost =
fleetSize × SUMPRODUCT(trips, driverCostPerTrip)
```

这已经确认原 Excel 是 **元/趟** 口径。

## R-VAR-002 过路费

``` text
segmentTollCost =
fleetSize × trips × tollPerTrip
```

## R-VAR-003 装卸费

``` text
segmentLoadingCost =
fleetSize × trips × loadingFeePerTrip
```

## R-VAR-004 信息费

``` text
segmentInformationCost =
fleetSize × trips × informationFeePerTrip
```

------------------------------------------------------------------------

# 6. 能源成本

这是原模型里一个非常关键的规则。

原 Excel 路段公式：

``` excel
=IF(载重,
    车辆数 × 满载能耗 × 电价 × 里程,
    车辆数 × 空载能耗 × 电价 × 里程
 ) × 趟数
```

系统等价：

``` text
consumption =
if loadTon is truthy / > 0
then loadedConsumption
else emptyConsumption

segmentEnergyCost =
fleetSize
× electricityPrice
× consumption
× distanceKm
× tripsPerVehicleMonth
```

### 关键问题

原表用 `IF(E8, 满载能耗, 空载能耗)` 判断满空载。

也就是说：

-   载重不为 0 → 满载能耗；
-   载重为 0 → 空载能耗。

这不是"去程满载 +
回程空载"的双程能源模型，而是**每个路段只能被视为满载或空载其中一种状态**。

### 产品化建议

V1 为了与 Excel 对账，可以实现：

``` text
load_state = LOADED if loadTon > 0 else EMPTY
```

但数据模型应预留：

``` text
load_state
loaded_distance_km
empty_distance_km
```

后续确认真实业务后升级。

------------------------------------------------------------------------

# 7. 轮胎成本

原 Excel：

``` text
routeMileagePerVehicle =
Σ(distance × trips)

routeTireCost =
routeMileagePerVehicle
/ tireLifeKm
× tireCount
× fleetSize
× tireUnitPrice
```

路段成本采用里程×趟数权重分摊。

系统可直接计算路段：

``` text
segmentTireCost =
distanceKm
× tripsPerVehicleMonth
× fleetSize
× tireCount
× tireUnitPrice
/ tireLifeKm
```

两种算法在线性条件下等价。

------------------------------------------------------------------------

# 8. 财务成本

## R-FIN-001 营收垫资成本

原 Excel：

``` text
revenueAdvanceCost =
monthlyRevenue
× revenueDiscountRate
/ 12
× receivableCycleMonths
```

例如：

``` excel
=AC39*$E$34/12*$E$26
```

### 产品解释

它实际把"营收年贴现率"作为应收账款资金占用成本率使用。

因此字段名建议暂时改为：

``` text
receivable_fund_cost_rate
```

而不是直接叫 discount_rate。

**待业务确认：**4% 到底是贴现率、资金机会成本还是垫资利率。

## R-FIN-002 流动资金贷款利息

月度线路口径：

``` text
workingCapitalInterest =
(
  monthlyRentAnnualized
  + otherOperatingCosts
)
× annualLoanRate / 12
× loanCycleMonths
```

年度现金流区域则使用当年经营成本作为基础。

原表字段说明：

``` text
流动资金贷款需求 = 月经营成本 × 周期
```

因此产品化解释：

``` text
workingCapitalPrincipal ≈ operatingCost × loanCycle
interest = operatingCost × annualRate / 12 × loanCycle
```

------------------------------------------------------------------------

# 9. 税务模型

## R-TAX-001 销项税

原 Excel税率固定为 9%：

``` text
outputVAT =
revenue × 9% / 1.09
```

说明原收入被视为**含税收入**。

系统禁止写死 9%，必须由 taxRuleVersion 提供。

## R-TAX-002 进项税

原 Excel：

``` text
inputVAT =
(vehicleCost + energyCost + tireCost) × 13% / 1.13
+ insuranceCost × 6% / 1.06
```

也就是说当前 Excel 只把以下项目纳入抵扣：

-   车辆成本：13%
-   能源成本：13%
-   轮胎成本：13%
-   车辆保险费：6%

其他成本当前没有进入这条进项税公式。

**这是必须业务/财务确认的规则。**

## R-TAX-003 应缴增值税

``` text
vatPayable = MAX(0, outputVAT - inputVAT)
```

原表不允许负数进入当期税务成本。

系统：

``` text
vatPayable = max(0, outputVAT - inputVAT)
```

如未来需要处理留抵税额，另建 carryForwardInputVAT 规则。

------------------------------------------------------------------------

# 10. 月利润

``` text
totalCost =
vehicleCost
+ managementFee
+ roadMaintenanceFee
+ maintenanceFee
+ inspectionFee
+ insuranceFee
+ parkingFee
+ heaterFee
+ consumableFee
+ driverCost
+ tollCost
+ loadingCost
+ informationCost
+ energyCost
+ tireCost
+ revenueAdvanceCost
+ workingCapitalInterest
+ vatPayable
```

``` text
monthlyProfit = monthlyRevenue - totalCost
```

路段利润：

``` text
segmentProfit =
segmentRevenue
- allocatedFixedCost
- segmentVariableCost
- allocatedFinanceCost
- segmentTaxCost
```

------------------------------------------------------------------------

# 11. 原 Excel 的路段成本分摊规则

这是系统实现时不能漏掉的一层。

固定成本、部分财务成本、税务成本在 Excel 中先计算线路总额，然后按：

``` text
distance × trips
```

进行路段分摊。

统一公式：

``` text
segmentAllocatedCost =
routeCost
× segmentDistance
× segmentTrips
/
Σ(distance × trips)
```

因此 Cursor 不应该把所有成本都简单"平均除以路段数"。

------------------------------------------------------------------------

# 12. 长期年度测算

原 Excel 后半部分按"年"生成长期经营模型。

年度收入：

``` text
annualRevenue =
fleetSize
× Σ(freightPrice × loadTon × tripsPerMonth)
× operatingMonthsYear
```

并根据租赁形式/折旧年限判断该年份是否继续运营。

### 纯租赁

当：

``` text
year × 12 <= installmentMonths
```

才继续产生对应经营收入/成本。

### 非纯租赁

主要按照：

``` text
year × 12 <= depreciationMonths
```

控制长期经营期。

这里说明原 Excel 把"分期月份/折旧月份"同时用于项目经营期限控制。

**待确认：真实项目运营期限是否应该独立字段。**

系统建议新增：

``` text
project_operating_months
```

不要长期依赖 installmentMonths/depreciationMonths 推断项目生命周期。

------------------------------------------------------------------------

# 13. 现金流

原 Excel存在两套现金流区域。

初始期：

``` text
initialVehicleCashOut = fleetSize × downPaymentPerVehicle
```

初始税务：

``` text
initialInputVAT =
initialVehicleCashOut × 13% / 1.13
```

初始净现金流大致为：

``` text
-currentVehicleInvestment + taxEffect
```

经营期：

``` text
currentCashFlow =
annualRevenue
- annualOperatingCosts
- VAT
```

累计：

``` text
cumulativeCashFlow(t) =
Σ(currentCashFlow[0..t])
```

### 开发要求

不要直接复制 Excel "第几列代表第几年"的方式。

建立：

``` text
CashFlowPeriod {
  periodIndex,
  periodType,
  revenue,
  operatingCost,
  capex,
  financing,
  tax,
  netCashFlow,
  cumulativeCashFlow
}
```

------------------------------------------------------------------------

# 14. IRR

原 Excel至少计算：

``` text
IRR 4年
IRR 5年
IRR 6年
IRR 8年
```

以及另一块 12 月 IRR。

系统实现：

``` text
calculateIRR(cashFlows[])
```

当现金流序列没有正负号变化或算法不收敛：

``` json
{
  "value": null,
  "reason": "IRR_NOT_CALCULABLE"
}
```

禁止展示 `#NUM!`。

------------------------------------------------------------------------

# 15. 敏感性分析

原 Excel 已确认包含围绕以下变量变化重算利润的逻辑：

-   电价
-   运价
-   单车月运营趟数
-   月租
-   满载能耗
-   装卸费
-   信息费等区域也存在参数扰动公式

产品化后统一：

``` text
SensitivityScenario {
  parameterCode,
  baseValue,
  changeType,
  changeValue,
  scenarioValue
}
```

每个 Scenario 必须重新调用：

``` text
calculateScheme(snapshot + override)
```

禁止：

``` text
newProfit = oldProfit × 某比例
```

------------------------------------------------------------------------

# 16. 建议的计算顺序

Cursor 必须按依赖顺序执行：

``` text
01 validateParameters()
02 calculateSegmentOperationalMetrics()
03 calculateSegmentRevenue()
04 calculateRouteRevenue()

05 calculateVehicleCost()
06 calculateManagementFee()
07 calculateOtherFixedCosts()

08 calculateDriverCost()
09 calculateTollCost()
10 calculateLoadingCost()
11 calculateInformationCost()
12 calculateEnergyCost()
13 calculateTireCost()

14 calculateRevenueAdvanceCost()
15 calculateWorkingCapitalInterest()

16 calculateOutputVAT()
17 calculateInputVAT()
18 calculateVATPayable()

19 calculateRouteTotalCost()
20 allocateSharedCostsToSegments()
21 calculateSegmentProfit()
22 calculateRouteProfit()

23 calculateAnnualProjection()
24 calculateCashFlow()
25 calculateIRR()

26 aggregateProjectResult()
```

------------------------------------------------------------------------

# 17. 建议 Result 数据结构

``` json
{
  "schemeVersionId": "xxx",
  "ruleVersion": "V1",
  "monthly": {
    "revenue": 0,
    "fixedCost": 0,
    "variableCost": 0,
    "financeCost": 0,
    "taxCost": 0,
    "totalCost": 0,
    "profit": 0,
    "profitMargin": null
  },
  "routes": [],
  "cashFlows": [],
  "investmentMetrics": {
    "irr4y": null,
    "irr5y": null,
    "irr6y": null,
    "irr8y": null
  }
}
```

每一个成本项目单独保存，不要只存 totalCost。

------------------------------------------------------------------------

# 18. 必须编写的对账测试

第一阶段最重要的验收不是页面，而是：

> **同一组输入参数，系统输出应与 Excel 输出一致。**

建立 `excel-parity` 测试集。

建议至少准备：

### Case 01 单路段纯租赁

-   1 条线路
-   1 个路段
-   有载重
-   固定电价
-   固定趟数

核对：

-   收入
-   能源成本
-   轮胎
-   固定成本
-   财务成本
-   税
-   利润

### Case 02 多路段

验证成本分摊：

``` text
distance × trips
```

### Case 03 空载路段

载重 = 0。

验证使用：

``` text
emptyConsumption
```

### Case 04 管理费边界

车辆：

``` text
29 / 30 / 49 / 50 / 99 / 100 / 199 / 200
```

### Case 05 非纯租赁

核对首付摊销和折旧周期。

### Case 06 长期现金流

核对 4/5/6/8 年 IRR。

允许由于 Decimal/Excel 精度存在极小误差，但需设置 tolerance。

------------------------------------------------------------------------

# 19. 已发现的原 Excel 风险点

开发时不要无脑复制：

### 风险1：运价单位文字可能写反

表中显示"吨/元"，公式表现为"元/吨"。

### 风险2：满空载判断过于简单

`载重 > 0 = 满载；载重 = 0 = 空载`。

不能表达一趟运输同时存在满载和返程空驶。

### 风险3：经营期限与融资/折旧期限耦合

项目生命周期应该考虑独立字段。

### 风险4：税率硬编码

9%、13%、6% 均直接存在 Excel 公式中。

### 风险5：部分成本按里程×趟数分摊

这是管理测算口径，不一定等于真实成本归属。

### 风险6：进项税可抵扣范围非常有限

需要财务确认。

### 风险7：Excel 中空输入会出现 #DIV/0!/#NUM!

系统必须全部转换成参数校验或明确的不可计算状态。

------------------------------------------------------------------------

# 20. V1 规则状态

  模块           状态
  -------------- -----------------------------------
  收入公式       已确认 Excel 公式
  司机成本       已确认：元/趟
  过路费         已确认：元/趟
  装卸费         已确认：元/趟
  信息费         已确认：元/趟
  能源成本       已确认 Excel 公式
  轮胎成本       已确认 Excel 公式
  管理费阶梯     已确认 Excel 公式
  固定成本分摊   已确认 Excel 公式
  营收垫资       已确认 Excel 公式，业务含义待确认
  流贷利息       已确认 Excel 公式，业务口径待确认
  销项税         已确认 Excel 公式
  进项税         已确认 Excel 公式，抵扣范围待确认
  月利润         已确认
  年度现金流     公式结构已确认
  IRR            已确认
  多线路权重     业务含义待确认

------------------------------------------------------------------------

# 21. 给 Cursor 的直接执行要求

请基于本规则文档和《项目测算工具 PRD V1.0》实现。

第一阶段优先保证：

``` text
计算正确 > 数据模型正确 > 可测试 > 页面完整 > 视觉效果
```

开发过程中：

1.  所有公式进入独立 Calculation Domain。
2.  使用 Decimal/高精度金额类型。
3.  RuleVersion 必须跟随结果保存。
4.  对账测试先于复杂 UI。
5.  所有待确认规则使用配置/策略模式隔离。
6.  不允许根据 Excel 单元格地址组织业务代码。
7.  不允许把 Excel 的 5线路、10路段限制带进系统。
8.  必须支持查看 calculation breakdown。
9.  敏感性分析复用同一个计算引擎。
10. Excel parity 测试通过后，再开始做第二阶段规则优化。
