# 项目测算系统 V1 验收问题清单 + Cursor 第二轮修改指令

## 1. 本轮目标

当前项目测算 V1 已完成主要产品骨架，本轮禁止大规模推翻现有架构。

本轮目标不是增加 AI、报表等新功能，而是：

1.  修复影响测算正确性的核心问题。
2.  修复影响实际业务使用的交互问题。
3.  补齐关键边界测试。
4.  确保计算结果可解释、可复算、可追溯。
5.  确保核心计算口径与原始 Excel 测算模型一致。
6.  完成本轮后，使 V1 达到可业务验收状态。

优先级原则：

> 计算正确 \> 数据模型正确 \> 数据可追溯 \> 可测试 \> 操作体验 \>
> 视觉优化

------------------------------------------------------------------------

## 2. P0：必须修复

### P0-01 运价单位必须进入正式计算逻辑

系统路段已经存在
`freightPriceUnit`，并允许配置不同运价单位，但正式收入计算不能继续默认所有运价都按照：

`车数 × 运价 × 载重 × 趟数`

计算。

V1 至少支持：

-   元/吨：`月收入 = 车队规模 × 单车月趟数 × 单趟载重 × 运价`
-   元/趟：`月收入 = 车队规模 × 单车月趟数 × 运价`
-   元/吨公里：`月收入 = 车队规模 × 单车月趟数 × 单趟载重 × 路段里程 × 运价`

收入计算必须统一进入 RevenueCalculator / RuleEngine。

禁止前端自己计算正式结果、API 单独复制公式、Sensitivity
重新写收入公式、Compare
自己重新推导。所有正式结果必须调用同一个计算引擎。

#### 空载路段

如果 `loadTon = 0`，则元/吨和元/吨公里模式下
`revenue = 0`，但能源、轮胎、司机、过路费等成本仍正常产生。

元/趟模式保留空载运输产生收入的业务能力，不要通过 `loadTon = 0`
强制归零。

#### 测试

至少增加：

-   元/吨收入测试
-   元/趟收入测试
-   元/吨公里收入测试
-   空载 + 元/吨
-   空载 + 元/趟
-   空载 + 元/吨公里

### P0-02 年运营月数层级统一

当前系统同时存在：

-   路段 `operatingMonthsYear`
-   方案/财务相关经营周期
-   `calculationYears`
-   `installmentMonths`
-   `depreciationMonths`
-   `projectOperatingMonths`

必须明确三个概念：

#### A. 年运营月数

表示一年实际运营多少个月，例如 12、10、8
个月，主要用于年收入、年运营成本、年运输量、年里程。

V1 建议作为**方案级参数**。如果当前数据库为了兼容已经在 Segment
保存，则正式计算时必须确定唯一来源，不允许不同路段随意出现不同值。

#### B. 项目经营月数

表示整个项目预计持续多少个月，例如 60、72、96 个月，主要用于现金流周期。

#### C. 测算年限

用于投资评价和结果展示，例如
4年、5年、6年、8年，不得和"年运营月数"混用。

验收要求：不得出现一个模块按 12 个月、另一个模块按 10
个月，而界面没有明确告诉用户的情况。

------------------------------------------------------------------------

## 3. P1：V1 正式验收前完成

### P1-01 路段编辑请求方式优化

当前路段参数输入过程中，每次 `onChange` 都立即：

`PUT API → refreshRoutes → 重新获取数据`

会导致大量网络请求、输入卡顿和请求乱序风险。

修改为：

`用户输入 → 更新前端本地 state → debounce 800ms → 保存`

推荐 debounce + blur 双保险。

用户连续输入 `360` 不能产生 3 次完整 refresh。

保存状态必须明确：

-   正在保存...
-   已自动保存
-   保存失败，请重试

不得静默失败。

### P1-02 建立 Excel 黄金样本测试

建立固定黄金测试数据，建议目录：

`/tests/golden/`

至少覆盖：

1.  单线路单路段纯租赁：收入、车辆成本、固定成本、能源、轮胎、司机、过路、装卸、信息费、财务成本、VAT、利润。
2.  多路段：重点验证共享成本按
    `(distance × trips) / Σ(distance × trips)`
    分摊，禁止按路段数量平均。
3.  空载路段：`loadTon = 0 → 使用 emptyEnergyConsumption`。
4.  管理费边界：29、30、49、50、99、100、199、200。
5.  非纯租赁：验证首付摊销 + 月租。
6.  长周期现金流：验证初始投资、月经营现金流、累计现金流、首次转正月份、IRR。

管理费标准：

-   `<30`：2000
-   `30-49`：1500
-   `50-99`：1200
-   `100-199`：1000
-   `>=200`：800

精度要求：

-   金额允许误差 `<= 0.01元`
-   百分比允许误差 `<= 0.0001`
-   禁止使用 JS float 直接处理正式财务金额

### P1-03 司机成本层级统一

当前同时存在：

-   `vehiclePlan.driverCost`
-   `driverCostType`
-   `segment.driverCostPerTrip`

V1 推荐：

如果 `driverCostType = PER_TRIP`：

-   `segment.driverCostPerTrip` 有值：使用路段覆盖值
-   没有：使用 `vehiclePlan.driverCost`

计算解释中必须说明"司机成本来源：路段覆盖值"或"司机成本来源：方案默认值"。

### P1-04 不同运价单位时禁止计算平均运价

如果一个方案同时存在元/吨、元/趟、元/吨公里，则这些数值不能直接平均。

单位全部一致时显示平均运价；单位不一致时显示"多计价口径"，并支持查看各计价口径数据。

### P1-05 IRR 异常处理

不得向用户展示：

-   NaN
-   Infinity
-   #NUM!

现金流不存在正负号变化时：

`irr = null`

`irrReason = IRR_NO_SIGN_CHANGE`

算法无法收敛时：

`IRR_NOT_CONVERGED`

UI 显示"无法计算"并给出解释。

至少测试正常
IRR、全正现金流、全负现金流、无符号变化、极端现金流、长周期现金流。

### P1-06 利润率除零保护

如果 `revenue = 0`：

`profitMargin = null`

`profitMarginReason = REVENUE_ZERO`

不得返回 0、NaN 或 Infinity。

UI 显示"无法计算"，解释为"当前方案营收为0，因此利润率无法计算"。

### P1-07 VAT 规则继续配置化

不得把 9%、13%、6% 散落硬编码到页面/API/各 Calculator。

必须由 `Rule Engine + Rule Version` 统一控制。

原 Excel 当前参考口径：

`销项VAT = Revenue × 9% / 1.09`

进项主要涉及车辆成本、能源成本、轮胎成本、保险，但必须允许规则版本调整。

------------------------------------------------------------------------

## 4. P2：不阻塞 V1，但建议处理

### P2-01 测算结果增加可视化

至少预留：

-   成本构成：车辆、固定运营、能源、轮胎、司机、路桥、装卸、信息、财务、税务。
-   线路利润贡献：各线路收入、成本、利润。
-   现金流趋势：月度现金流、累计现金流。

V1 不强制完成。

### P2-02 参数来源标签统一

参数建议统一显示：

-   公司标准
-   项目填写
-   线路填写
-   路段填写
-   系统计算
-   人工覆盖

覆盖标准值时必须填写调整原因，正式测算参数快照必须保存调整原因。

------------------------------------------------------------------------

## 5. 正式测算必须冻结快照

点击"开始测算"后必须生成不可变 `parameter_snapshot`，至少包含：

-   scheme
-   routes
-   segments
-   vehiclePlan
-   financeTaxPlan
-   standardParameters
-   overrides
-   ruleVersion
-   calculationTime

结果必须绑定：

-   snapshotId
-   ruleVersion

正确流程：

`草稿参数 → 开始测算 → 冻结 Snapshot V1 → Rule Engine → Result V1`

修改参数后：

`Snapshot V2 → Result V2`

不得重新解释或覆盖历史结果。

------------------------------------------------------------------------

## 6. 结果可解释性

所有核心结果必须支持：

-   resultCode
-   resultName
-   resultValue
-   unit
-   ruleCode
-   ruleVersion
-   calculationExpression
-   explanation
-   sourceParameterSnapshot

例如能源成本
¥108,000，点击后应展示车辆数、电价、满载能耗、里程、月趟数以及完整公式：

`30 × 0.6 × 1.5 × 200 × 20 = ¥108,000`

禁止只保存最终金额。

------------------------------------------------------------------------

## 7. 敏感性分析验收

必须使用：

`baseline snapshot → clone → override parameter → calculateScheme() → scenario result`

禁止使用：

`baselineProfit × 变化百分比`

至少支持：

-   运价
-   电价
-   月趟数
-   月租
-   满载能耗

同时支持 `PERCENT` 和 `ABSOLUTE`。

例如运价测试：-10%、-5%、0、+5%、+10%。

0% 结果必须与 baseline 完全一致。

------------------------------------------------------------------------

## 8. 完整用户链路验收

完成修改后必须验证：

`创建项目 → 进入项目 → 创建测算方案 → 填写基础信息 → 新增线路 → 新增路段 → 填写运输参数 → 填写车辆参数 → 填写财务税务 → 参数校验 → 正式测算 → 查看结果 → 查看计算解释 → 查看现金流 → 执行敏感性分析 → 复制方案 → 修改参数 → 重新测算 → 方案对比 → 设为基准方案 → 查看历史版本`

任何一步失败都记录测试结果。

------------------------------------------------------------------------

## 9. 本轮禁止做的事情

第二轮不要开发：

-   AI 智能对话
-   OCR
-   尽调文档识别
-   AI 风险报告
-   AI 自动补参
-   AI 方案推荐
-   大规模视觉重构
-   TMS 数据自动接入
-   复杂权限体系
-   审批流

这些进入 V2。

当前唯一目标：

> 把项目测算 V1 的计算底座打稳。

------------------------------------------------------------------------

## 10. 完成后必须输出测试报告

请生成：

`docs/V1_SECOND_ROUND_ACCEPTANCE.md`

### 修改结果

  ID      问题       状态        修改文件   说明
  ------- ---------- ----------- ---------- ------
  P0-01   运价单位   PASS/FAIL   xxx        xxx
  P0-02   运营月数   PASS/FAIL   xxx        xxx

### 自动化测试结果

记录：

-   Test Suites
-   Tests
-   Passed
-   Failed
-   Duration

### 黄金样本

  指标       Excel   System   Difference Result
  -------- ------- -------- ------------ --------
  月营收                                 PASS
  月成本                                 PASS
  月利润                                 PASS
  VAT                                    PASS
  IRR                                    PASS

### 未解决问题

不得隐藏，全部列出：

-   问题
-   原因
-   影响
-   建议
-   是否阻塞 V1

------------------------------------------------------------------------

## 11. 最终验收标准

### A. 计算正确

核心 Excel 黄金样本通过。

### B. 边界正确

0值、空载、不同计价单位、管理费边界、IRR异常全部通过。

### C. 结果稳定

不得出现：

-   NaN
-   Infinity
-   undefined
-   #DIV/0!
-   #NUM!

### D. 可解释

核心财务指标能够追溯：

`参数 → 公式 → 规则版本 → 结果`

### E. 可复算

相同 `Snapshot + Rule Version` 必须得到完全相同结果。

### F. 可版本化

历史正式测算结果不得被后续参数修改污染。

------------------------------------------------------------------------

## 12. Cursor 执行要求

请先阅读当前仓库代码和已有测试，再修改，不要重新搭建项目。

优先复用：

-   RuleEngine
-   calculateScheme
-   RevenueCalculator
-   EnergyCostCalculator
-   FixedCostCalculator
-   VariableCostCalculator
-   FinanceCostCalculator
-   TaxCalculator
-   CashFlowCalculator
-   InvestmentCalculator
-   SensitivityAnalysisService

如果当前实际类名不同，以现有实现为准。

修改原则：

`最小必要改动 + 统一计算入口 + 补测试 + 避免重复公式`

完成代码修改后：

1.  执行 lint。
2.  执行 type check。
3.  执行全部自动化测试。
4.  执行新增边界测试。
5.  执行 Excel 黄金样本。
6.  检查 build。
7.  输出 `V1_SECOND_ROUND_ACCEPTANCE.md`。
8.  明确列出仍未解决的问题。

禁止在测试未通过时声称 V1 已完成。

------------------------------------------------------------------------

## 13. Definition of Done

只有同时满足以下条件才算第二轮完成：

-   P0 全部关闭
-   P1 核心计算问题全部关闭
-   Golden Test 通过
-   Build 通过
-   Type Check 通过
-   核心边界 Case 通过
-   正式结果快照不可变
-   计算解释可追溯
-   敏感性 0% 与 baseline 一致
-   未发现 NaN / Infinity
-   输出第二轮验收报告

完成以上工作后停止继续增加功能，等待产品验收。
