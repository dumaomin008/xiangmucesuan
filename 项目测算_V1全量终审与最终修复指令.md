# 项目测算 V1 全量终审与最终修复指令

> 版本定位：V1 最终收口\
> 执行对象：Cursor\
> 原则：不扩需求、不重构无关模块；先验证，再修复；所有 PASS
> 必须有源码或测试证据。

## 一、最终目标

本轮不是继续增加功能，而是对现有 V1
做一次完整收口，使系统达到"可以进入真实业务项目验收"的状态。

验收主链路：

``` text
项目
→ 创建测算方案
→ 基础信息
→ 线路
→ 路段
→ 运输参数
→ 运力成本
→ 财务税务
→ 参数校验
→ 正式测算
→ 结果
→ 公式解释
→ 现金流
→ IRR / 回收期
→ 敏感性分析
→ 复制方案
→ 修改参数
→ 重新测算
→ 方案对比
→ 基准方案
→ 历史版本
```

本轮优先级：

``` text
P0：结果错误、历史被污染、核心链路不可用
P1：上线/业务验收前应修
P2：体验和工程优化，可进入后续版本
BUSINESS：必须由业务确认，禁止开发自行猜测
```

------------------------------------------------------------------------

# 二、已经重点复验通过的核心能力

以下能力已有专项源码复验基础，本轮只做回归，不得随意重写：

1.  运价单位正式进入收入计算：

    -   PER_TON
    -   PER_TRIP
    -   PER_TON_KM

2.  空载收入边界：

    -   PER_TON / PER_TON_KM：载重为 0 时收入为 0
    -   PER_TRIP：可正常产生收入

3.  年运营月数已经提升到方案/财务测算层级。

4.  路段编辑已从逐键请求调整为 debounce/保存状态方向。

5.  司机成本支持路段覆盖方案默认值。

6.  混合运价单位不再直接计算无意义的平均运价。

7.  IRR 已处理：

    -   无符号变化
    -   不收敛
    -   NaN / Infinity

8.  营收为 0 时利润率不做除零。

9.  正式测算创建不可变 Parameter Snapshot，结果绑定 snapshotId +
    ruleVersion。

10. 敏感性分析采用"基线快照 → override → 完整重算"，不是利润乘百分比。

11. Month 0 已进入现金流。

12. 初始投资已进入累计现金流和 IRR。

13. 项目周期已经改成月级判断，支持 18/30/42/54 等非整年周期。

14. 年度现金流改为从月度现金流聚合。

15. 非纯租赁尾期租金按 monthIndex \<= installmentMonths 判断，避免 30
    个月分期在后续年份重复计提。

16. VAT CARRY_FORWARD 已进入月度现金流，负 VAT 默认不直接形成现金流入。

17. 已增加现金流独立手算 Case，包括：

-   -180 万初始投资、每月 +10 万，第 18 月累计为 0
-   18/30 月项目周期
-   30/42/54 月租金
-   VAT 留抵

这些内容本轮必须做回归测试，但不要为了"优化代码"再次改写已经稳定的核心实现。

------------------------------------------------------------------------

# 三、P0 全量终审

## P0-01 核心结果守恒校验

对每次正式测算增加/确认以下 invariant 测试。

### 收入

``` text
方案月收入
=
Σ 路段月收入
```

### 成本

``` text
方案总成本
=
固定成本
+ 变动成本
+ 财务成本
+ 税费
```

### 路段成本

``` text
Σ 路段分摊成本
=
方案对应可分摊成本
```

特别检查司机成本。

历史审核曾发现过：

``` text
方案 monthlyVariableCost 包含 driverCost
但 route variableCost 汇总可能遗漏 driverCost
```

必须在当前最新代码重新验证。

如果仍存在，P0 修复。

### 利润

``` text
利润
=
收入 - 总成本
```

### 利润率

``` text
收入 > 0：
利润率 = 利润 / 收入

收入 = 0：
profitMargin = null
```

不得出现：

``` text
NaN
Infinity
-Infinity
undefined
```

------------------------------------------------------------------------

# 四、P0-02 Excel V5 对齐

保留原始 Excel 示例作为不可变 Golden Source。

核心固定值至少继续验证：

``` text
月营收 = 172260
车辆成本 = 33360
管理费 = 4800
维护费 = 11000
年检费 = 800
保险费 = 6000
加热费 = 800
易耗品 = 480
司机成本 = 27000
过路费 = 21060
信息费 = 7200
能源成本 = 25391.232
轮胎成本 = 6076.62
流动资金利息 = 839.81247
销项 VAT = 14223.3027522936
进项 VAT = 7797.69411053598
应纳 VAT = 6425.60864175759
总成本 = 151233.273111758
利润 = 21026.7268882424
```

金额误差：

``` text
<= 0.01
```

百分比误差：

``` text
<= 0.0001
```

禁止用：

``` text
calculateA()
```

生成 expected，再用：

``` text
calculateB()
```

比较。

至少一组核心 Golden expected 必须来自固定 Excel 值。

------------------------------------------------------------------------

# 五、P0-03 路段成本分摊

共享成本权重继续按已确认 Excel 逻辑：

``` text
segmentWeight
=
(distance × trips)
/
Σ(distance × trips)
```

禁止：

``` text
固定成本 / 路段数量
```

检查：

-   1 路段
-   2 路段
-   5 路段
-   distance=0
-   trips=0
-   全部 distance×trips=0

最后一种情况不得除零。

必须明确 fallback。

如果业务没有确认 fallback，返回 validation error，不要自行平均分摊。

------------------------------------------------------------------------

# 六、P0-04 正式测算与预览必须同源

检查以下所有位置：

``` text
配置页实时预览
正式测算
结果页
方案对比
敏感性分析
历史版本
```

核心公式不得各写一套。

收入必须统一调用 RevenueCalculator/等价统一实现。

能源、固定成本、变动成本、财务、VAT、现金流同理。

如果发现前端自行复制正式计算公式：

P0/P1 根据影响修复。

前端允许格式化，不允许重新定义业务公式。

------------------------------------------------------------------------

# 七、P0-05 Snapshot 不可变性

正式测算：

``` text
Input
→ ParameterSnapshot
→ CalculationResult
```

要求：

1.  每次正式测算生成新的 Snapshot。
2.  历史 Snapshot 不 UPDATE。
3.  CalculationResult 绑定 snapshotId。
4.  CashFlow 绑定对应结果/快照。
5.  Rule Version 固化。
6.  后续修改方案参数不改变历史结果。
7.  历史结果打开时使用历史 Snapshot，不使用当前方案实时参数。

增加集成测试：

``` text
测算 A
→ 修改运价
→ 测算 B
→ 查询 A
```

A 的：

``` text
参数
收入
成本
利润
现金流
ruleVersion
```

必须完全不变。

------------------------------------------------------------------------

# 八、P0-06 方案复制

全量检查 Copy Scheme。

复制后必须：

复制：

``` text
方案基础信息
线路
路段
运力方案
财务税务参数
标准参数 override
```

不得复制成当前有效历史结果：

``` text
CalculationResult
ParameterSnapshot
历史版本
旧 CashFlow
```

新方案必须重新正式测算。

同时检查 ID：

``` text
routeId
segmentId
schemeId
```

必须重新生成，不能引用原方案子记录。

------------------------------------------------------------------------

# 九、P0-07 删除与级联

检查 Prisma Schema 和 API：

``` text
Project
CalculationScheme
CalculationRoute
CalculationSegment
VehiclePlan
FinanceTaxPlan
ParameterOverride
ParameterSnapshot
CalculationResult
CashFlow
CalculationSchemeVersion
SensitivityResult
```

确认删除行为。

历史测算已经存在时，不允许因为删除/修改当前配置导致：

``` text
Snapshot 丢失
Result 失去引用
历史版本无法打开
```

如果采用软删除/Archive：

所有列表/API必须保持一致。

------------------------------------------------------------------------

# 十、P0-08 正式测算重复提交

检查用户连续点击：

``` text
开始测算
开始测算
开始测算
```

是否产生无意义的多个重复 Snapshot / Result。

前端至少：

``` text
calculating=true
按钮 disabled
```

API 推荐有合理幂等/重复请求保护。

如果无法做严格幂等，至少保证 UI 不可重复触发，并确保失败后状态恢复。

------------------------------------------------------------------------

# 十一、P0-09 现金流最终回归

必须重新验证：

### Month 0

``` text
初始首付
初始 VAT 留抵
累计现金流
```

### 非整年周期

``` text
18
30
42
54
```

### 年运营月数

例如：

``` text
projectOperatingMonths=30
operatingMonthsYear=10
```

### 分期

``` text
30个月 = 12+12+6
42个月 = 12+12+12+6
54个月 = 12+12+12+12+6
```

### VAT

CARRY_FORWARD：

``` text
M1:
Output=50000
Input=80000
VAT Cash Out=0
Closing Credit=30000

M2:
Output=70000
Input=20000
Opening Credit=30000
VAT Cash Out=20000
Closing Credit=0
```

------------------------------------------------------------------------

# 十二、P0-10 IRR 与投资回收

IRR 输入必须包含：

``` text
Year 0 初始投资
```

无符号变化：

``` text
IRR = null
errorCode = IRR_NO_SIGN_CHANGE
```

不收敛：

``` text
IRR = null
errorCode = IRR_NOT_CONVERGED
```

禁止 UI：

``` text
NaN%
Infinity%
```

投资回收：

``` text
firstPositiveMonth
```

必须基于包含 Month 0 的累计现金流。

------------------------------------------------------------------------

# 十三、P1-01 初始投资为 0 的展示口径

当前数学上：

``` text
Month0 cumulative = 0
firstPositiveMonth = 0
```

但产品层不建议显示：

``` text
首次转正：0期
```

建议：

``` text
initialInvestment = 0
→ “无需回收初始投资”
```

不要改底层 firstPositiveMonth 数学定义。

只调整展示语义。

------------------------------------------------------------------------

# 十四、P1-02 输入参数边界

全量检查 API，不只前端。

以下原则上不得为负：

``` text
fleetSize
distanceKm
loadTon
tripsPerVehicleMonth
freightPrice
electricityPrice
loadedEnergyConsumption
emptyEnergyConsumption
monthlyRent
downPayment
managementFee
maintenanceFee
inspectionFee
insuranceFee
parkingFee
heaterFee
consumableFee
driverCost
tollCost
loadingCost
infoCost
tireLifeKm
tireCount
tirePrice
receivableCycleMonths
loanCycleMonths
installmentMonths
projectOperatingMonths
calculationYears
```

税率/利率按业务允许范围校验。

禁止只依赖 HTML input min。

API 必须校验。

------------------------------------------------------------------------

# 十五、P1-03 分母为 0

全仓搜索所有：

``` text
.div()
/
```

重点检查：

``` text
利润率
成本占比
路段权重
标准参数偏差率
同比/差异率
单位成本
单位收入
敏感性变化率
```

所有 denominator=0 必须有明确行为。

------------------------------------------------------------------------

# 十六、P1-04 运价单位一致性

PER_TON：

``` text
fleet × trips × loadTon × freightPrice
```

PER_TRIP：

``` text
fleet × trips × freightPrice
```

PER_TON_KM：

``` text
fleet × trips × loadTon × distanceKm × freightPrice
```

方案对比：

同单位：

可展示平均运价。

混合单位：

``` text
“多计价口径”
```

禁止直接平均。

------------------------------------------------------------------------

# 十七、P1-05 司机成本来源解释

如果：

``` text
driverCostType = PER_TRIP
```

优先级：

``` text
segment.driverCostPerTrip
>
vehiclePlan.driverCost
```

结果解释必须告诉用户来源：

``` text
路段覆盖值
```

或：

``` text
方案默认值
```

------------------------------------------------------------------------

# 十八、P1-06 标准参数 Override

检查：

``` text
标准参数
→ 项目/方案引用
→ 手工覆盖
→ 覆盖原因
→ Snapshot
→ Result Explanation
```

覆盖必须：

1.  保存 value。
2.  保存 source。
3.  保存 reason。
4.  正式测算冻结。
5.  历史可追溯。

禁止只有页面提示，没有快照记录。

------------------------------------------------------------------------

# 十九、P1-07 规则版本

以下规则不得散落硬编码：

``` text
管理费阶梯
VAT 销项税率
VAT 进项税率
保险税率
负 VAT 处理
```

结果必须能追踪：

``` text
ruleCode
ruleVersion
```

不要在页面/API/Calculator 多处写：

``` text
0.09
0.13
0.06
```

------------------------------------------------------------------------

# 二十、P1-08 Sensitivity

每个敏感性 Scenario：

``` text
Baseline Snapshot
→ Clone
→ Override
→ calculateScheme()
→ Scenario Result
```

支持至少：

``` text
运价
电价
趟数
租金
满载电耗
```

0%：

``` text
Scenario Result == Baseline Result
```

不仅利润相等。

至少验证：

``` text
revenue
cost
profit
profitMargin
```

------------------------------------------------------------------------

# 二十一、P1-09 版本与基准方案

检查：

``` text
复制方案
历史版本
设置基准
方案对比
```

基准方案只能有一个有效状态。

设置新基准时旧基准应取消。

历史 Version 不允许随当前 Scheme 修改。

------------------------------------------------------------------------

# 二十二、P1-10 页面完整链路

人工/自动逐页检查。

## 项目列表

-   新建
-   编辑
-   查看
-   空状态
-   异常状态

## 项目详情

-   基础信息
-   测算入口
-   方案列表

## 方案列表

-   新建
-   复制
-   归档
-   基准
-   对比

## 方案配置

5 步流程全部检查：

``` text
基础信息
线路运输
运力成本
财务税务
确认并测算
```

检查：

-   下一步
-   上一步
-   自动保存
-   保存失败
-   刷新恢复
-   必填校验
-   错误定位

## 线路/路段

-   新建
-   编辑
-   删除
-   复制
-   排序
-   debounce
-   保存状态

## 结果

-   KPI
-   成本拆分
-   路段结果
-   公式解释
-   无结果
-   计算失败

## 现金流

-   Month 0
-   月度
-   年度
-   首次转正
-   VAT 留抵

## 敏感性

-   参数
-   区间
-   step
-   0%
-   结果

## 对比

-   最多 3 个方案
-   混合运价
-   基准标识
-   缺结果方案

## 历史版本

-   历史结果不可变
-   可打开
-   参数可追踪

------------------------------------------------------------------------

# 二十三、P1-11 API 全量检查

覆盖：

``` text
/projects
/calculation-schemes
/calculation-routes
/calculation-segments
/calculation-versions
/sensitivity
/standard-parameters
/meta
```

以及所有：

``` text
[id]
copy
archive
calculate
results
cash-flow
routes
reorder
```

检查：

``` text
GET
POST
PUT/PATCH
DELETE
```

重点：

-   404
-   400
-   409（如适用）
-   500
-   非法 JSON
-   缺必填字段
-   不存在 ID
-   跨 Scheme 子资源修改
-   重复提交

API 不得静默吞异常。

------------------------------------------------------------------------

# 二十四、P1-12 数据归属校验

重点防止：

用户知道一个：

``` text
segmentId
```

就能通过 API 把它错误挂到其他 Scheme/Route。

所有子资源操作必须验证父子关系。

例如：

``` text
segment.routeId
route.schemeId
scheme.projectId
```

必须一致。

即使 V1 暂无复杂权限，也要保证数据关系不被破坏。

------------------------------------------------------------------------

# 二十五、P1-13 前后端字段使用审计

对 Prisma 字段建立矩阵：

  ---------------------------------------------------------------------------------
  字段       UI输入     API保存    Snapshot   Calculator   Result     Explanation
  ---------- ---------- ---------- ---------- ------------ ---------- -------------

  ---------------------------------------------------------------------------------

重点找：

``` text
UI 有字段但 Calculator 没用
DB 有字段但从不读取
Calculator 需要但 Snapshot 没冻结
Result 展示了但来源不是 Snapshot
```

发现后分类：

``` text
USED
DISPLAY_ONLY
RESERVED
DEPRECATED
BUG
```

禁止保留"看起来能配置、实际上不参与计算"的字段而不提示用户。

------------------------------------------------------------------------

# 二十六、特别检查 route.weight

历史审核发现：

``` text
route.weight
```

存在于数据/校验/页面，但未确认它是否真正属于核心计算规则。

原 Excel 汇总存在五组 20%，但业务含义尚未确认。

因此：

> 禁止因为字段名叫 weight 就自行把它加入收入或成本计算。

如果当前 V1 没有确认用途：

``` text
标记 RESERVED
```

或页面隐藏。

不要开发自行解释。

------------------------------------------------------------------------

# 二十七、P1-14 金额精度

金融金额禁止依赖 JS 浮点累计。

继续统一 Decimal。

重点：

``` text
金额乘除
成本汇总
税
现金流
IRR输入
敏感性
```

最终展示允许 round。

内部计算不要过早 round。

------------------------------------------------------------------------

# 二十八、P1-15 错误可解释性

正式测算失败不能只：

``` text
计算失败
```

至少返回：

``` text
errorCode
field/path
message
```

例如：

``` text
NO_ENABLED_SEGMENT
INVALID_FREIGHT_PRICE
ZERO_ALLOCATION_DENOMINATOR
IRR_NO_SIGN_CHANGE
IRR_NOT_CONVERGED
```

------------------------------------------------------------------------

# 二十九、P2 工程项

以下不阻塞 V1 业务验收，但记录：

1.  SQLite 不建议作为未来多人生产环境最终数据库。
2.  权限体系后续完善。
3.  并发编辑后续增加乐观锁/version。
4.  结果图表继续优化。
5.  操作审计日志。
6.  大数据量性能。
7.  CI 独立环境自动执行 Golden Test。
8.  E2E 浏览器测试。
9.  数据备份与恢复。
10. 导入/导出能力。

不要在本轮为了这些内容大规模重构。

------------------------------------------------------------------------

# 三十、BUSINESS 待确认项

以下内容 Cursor 禁止自行决定：

1.  原 Excel 汇总中的五组 20% 权重业务含义。
2.  司机成本未来是否统一按趟。
3.  维护费最终按月还是按公里。
4.  路桥/年检/保险的真实现金发生时点。
5.  4% revenue discount 的业务定义。
6.  流动资金占用本金最终口径。
7.  VAT 可抵扣范围和税务最终规则。
8.  RECOGNIZE_NEGATIVE 是否真实存在退税场景。
9.  租赁类型最终枚举及各类型合同公式。
10. distanceKm 是单程、运输段里程还是实际运营里程。
11. 满载/空载里程未来是否需要拆分。
12. 信息费最终单位。
13. 敏感性步长最终按百分比还是绝对值。
14. 多路段是否每一段都独立产生运输收入。
15. 项目经营期与分期/折旧期的业务关系。

这些项在系统中如未确认：

``` text
保持当前已确认 V1 规则
+ 明确注释
+ 不擅自扩展
```

------------------------------------------------------------------------

# 三十一、最终测试矩阵

至少覆盖：

### Revenue

-   PER_TON
-   PER_TRIP
-   PER_TON_KM
-   load=0
-   distance=0
-   trips=0
-   fleet=0

### Fixed Cost

-   fleet 0/1/N
-   纯租赁
-   非纯租赁
-   管理费 29/30/49/50/99/100/199/200

### Variable Cost

-   driver
-   toll
-   loading
-   info
-   energy
-   tire

### Allocation

-   单路段
-   多路段
-   0 权重

### Tax

-   正常 VAT
-   input \> output
-   carry forward
-   credit 用尽

### Cash Flow

-   Month0
-   18/30/42/54 月
-   30/42/54 分期

### IRR

-   正常
-   全正
-   全负
-   无符号变化
-   极端值
-   长周期

### Snapshot

-   A 测算
-   修改
-   B 测算
-   A 不变

### Sensitivity

-   -10%
-   0%
-   +10%
-   ABSOLUTE

### Compare

-   同运价单位
-   混合单位
-   1/2/3 方案

------------------------------------------------------------------------

# 三十二、禁止项

本轮不要开发：

-   AI 对话
-   OCR
-   尽调文件解析
-   AI 风险报告
-   AI 自动补参
-   AI 推荐
-   大屏
-   TMS 自动数据接入
-   复杂权限
-   审批流
-   大规模视觉重构

V1 先把测算引擎和业务链路收口。

------------------------------------------------------------------------

# 三十三、最终验收报告

完成后必须生成：

``` text
docs/V1_FINAL_AUDIT_ACCEPTANCE.md
```

结构：

## 1. 总结

``` text
P0 数量：
P0 已关闭：
P1 数量：
P1 已关闭：
P2：
BUSINESS：
是否具备业务验收条件：YES / NO
```

## 2. 问题清单

  ID   模块   问题   等级   修复   文件   测试   状态
  ---- ------ ------ ------ ------ ------ ------ ------

## 3. 全页面验收

  页面   新建   编辑   删除   保存   异常   空状态   PASS
  ------ ------ ------ ------ ------ ------ -------- ------

## 4. API 验收

  API   Method   正常   非法参数   不存在ID   归属校验   PASS
  ----- -------- ------ ---------- ---------- ---------- ------

## 5. Excel Golden

  指标     Excel   System   Diff PASS
  ------ ------- -------- ------ ------

## 6. 计算守恒

  Invariant   Expected   Actual   PASS
  ----------- ---------- -------- ------

## 7. 自动测试

``` text
Test Suites:
Tests:
Passed:
Failed:
Duration:
```

并记录：

``` text
typecheck
lint
build
```

真实执行结果。

------------------------------------------------------------------------

# 三十四、最终 Definition of Done

只有同时满足以下条件才允许写：

``` text
V1 READY FOR BUSINESS ACCEPTANCE
```

条件：

-   所有 P0 = 0
-   Excel 固定 Golden 通过
-   收入/成本/利润守恒
-   路段汇总与方案汇总一致
-   3 种运价通过
-   现金流通过
-   VAT 留抵通过
-   IRR 边界通过
-   Snapshot 不可变通过
-   Sensitivity 0% 与基线一致
-   Copy Scheme 不污染历史
-   历史版本可追溯
-   API 归属关系正确
-   核心输入后端校验完成
-   无 NaN / Infinity / undefined
-   Type Check PASS
-   Tests PASS
-   Build PASS
-   无未关闭 P0

如果存在任何未关闭 P0：

``` text
V1 NOT READY
```

不要为了完成任务把问题降级。

------------------------------------------------------------------------

# 三十五、Cursor 执行要求

执行顺序必须是：

``` text
1. 全仓审计
2. 输出发现的问题
3. P0 修复
4. P1 必要修复
5. 补测试
6. 全量回归
7. Excel Golden
8. Type Check
9. Lint
10. Build
11. 生成 V1_FINAL_AUDIT_ACCEPTANCE.md
12. 停止开发
```

不要边审计边扩功能。

不要修改 BUSINESS 待确认规则。

不要删除现有 Golden Test 来让测试通过。

不要降低断言精度来让测试通过。

不要把失败测试 skip。

不要用 mock 结果代替真实 Calculator。

本轮结束后停止继续编码，等待最终产品验收。
