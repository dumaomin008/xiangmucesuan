# 项目测算系统前端 UX/UI 全量重构实施方案 V1.0

> 目标：在不破坏现有计算引擎、公式、数据模型和已验证业务逻辑的前提下，将当前偏“模型/Excel 操作”的项目测算系统，重构为业务人员可快速理解、快速填报、快速检查、快速得到决策结论的测算工作台。

---

## 1. 本轮重构原则

### 1.1 核心原则

1. **计算逻辑不重写**：现有已验证计算函数、公式、参数依赖关系优先原样复用。
2. **重构的是交互层，不是测算模型**：禁止为了页面美观自行新增、删除或改变参与计算的业务参数。
3. **业务语言替代模型语言**：普通用户看到“单程距离、单趟载重、装货时间”，而不是参数编码、变量名、Excel 单元格或公式字段。
4. **输入即反馈**：关键参数修改后，应立即展示对核心测算指标的影响；最终正式结果仍由统一计算引擎生成。
5. **渐进式披露**：常用参数默认展示，高级参数、模型明细、中间变量收进“专业模式/高级设置”。
6. **AI 只辅助，不替代确定性计算**：AI 用于资料提取、补参建议、异常检查、解释、摘要；车辆数、成本、利润、现金流等结果必须来自计算引擎。
7. **Demo 可完整演示，正式版可继续演进**：禁止实现一次性、不可扩展的纯静态演示页面。

### 1.2 禁止事项

- 禁止将示意图中的样例数字硬编码为正式结果。
- 禁止 AI 直接生成最终财务结果并覆盖计算引擎。
- 禁止删除现有可用计算能力。
- 禁止修改现有公式后不做回归验证。
- 禁止普通模式一次性展示全部模型参数。
- 禁止让用户理解内部参数 ID、公式变量、Excel 行列映射后才能完成测算。

---

# 2. 产品信息架构

普通用户一级导航统一为：

1. **工作台**
2. **项目测算**
3. **方案对比**
4. **历史项目**
5. **测算模型**（有权限用户）

其中“项目测算”内部采用 5 步向导：

> 基本信息 → 运输场景 → 运营参数 → 成本收益 → 确认测算

完成后进入：

> 测算结果 → 调整参数 / 创建对比方案 → 方案对比

---

# 3. 全局页面框架

## 3.1 左侧导航

宽度建议 200~224px，支持收起。

菜单：

- 工作台
- 项目测算
- 方案对比
- 历史项目
- 测算模型

底部：

- 帮助中心
- 当前用户

## 3.2 顶部区域

项目测算过程中顶部固定展示：

- 项目名称
- 当前方案名称
- 保存状态：已保存 / 保存中 / 保存失败
- 参数完整度
- 普通模式 / 专业模式（按权限）
- 更多操作

## 3.3 页面宽度

建议：

- 主内容区最大宽度：1440px
- 普通表单内容：760~900px
- AI 辅助侧栏：300~360px
- PC 端优先适配 1440、1680、1920 宽度

---

# 4. 页面一：工作台 / 项目测算中心

## 4.1 页面目标

用户进入系统后，第一时间完成以下任一动作：

- 创建新测算
- 上传项目资料让 AI 识别
- 继续最近项目
- 查看已有测算结果

禁止首页堆砌与用户当前任务无直接关系的大量统计图。

## 4.2 页面结构

### A. 欢迎区

标题：

> 今天要测算什么项目？

提供两个主入口：

#### AI 创建项目

大输入框：

> 描述项目情况，例如：云南某钢材运输项目，年运量 120 万吨，单程 42km，使用新能源重卡……

能力：

- 文本输入
- 上传 PDF / Excel / Word / 图片（按当前项目实际支持能力接入）
- AI 开始分析

#### 手动新建

按钮：

> + 手动新建测算

进入 Step 1。

### B. 最近项目

表格字段：

- 项目名称
- 当前状态
- 当前方案
- 核心结果（已有结果时）
- 更新时间
- 操作

状态建议：

- 草稿
- 待补充
- 待确认
- 测算完成

操作：

- 继续测算
- 查看结果
- 复制项目
- 更多

---

# 5. 页面二：Step 1 / 项目基本信息

## 5.1 页面目标

只建立测算所需的项目级基本上下文，不提前暴露复杂模型参数。

## 5.2 顶部步骤条

固定：

1. 基本信息
2. 运输场景
3. 运营参数
4. 成本收益
5. 确认测算

当前步骤高亮，已完成步骤打勾。

## 5.3 表单区

字段必须优先映射现有系统字段。示例布局：

- 项目名称 *
- 项目类型 / 业务类型（若现有模型存在）
- 货物类型（若存在）
- 预计运输量及单位
- 测算周期 / 项目周期
- 运输方式（若存在）
- 项目描述

不得因本 PRD 示例而新增计算字段。

## 5.4 AI 资料识别卡片

如果用户通过 AI 创建：

右侧展示：

- 已识别字段
- 识别值
- 来源
- 置信提示
- 待确认字段

用户必须可以修改 AI 提取值。

AI 提取值进入正式计算前，应写入正常参数状态，不允许建立另一套 AI 专用计算数据。

## 5.5 操作

底部固定：

- 保存草稿
- 下一步

下一步前只校验本步骤必填项。

---

# 6. 页面三：Step 2 / 运输场景

## 6.1 页面目标

让业务人员描述“货从哪里到哪里、一次运多少、怎么组织运输”，隐藏底层复杂线路数据结构。

## 6.2 普通模式布局

采用“路线卡片 + 关键参数”结构。

示例：

```text
装货地
   ↓
运输距离
   ↓
卸货地
```

若当前模型已有多段线路能力，则按实际数据结构展示多个节点；不要强行简化导致模型信息丢失。

## 6.3 参数区

优先展示对运力影响最大的现有字段，例如：

- 装货地
- 卸货地
- 单程距离
- 返程距离
- 单趟运输量 / 核定载重
- 每日运营时长
- 运输组织方式

实际字段以代码和模型现状为准。

## 6.4 实时摘要

右侧或底部展示：

> 当前运输场景：每趟约 XX km，运输 XX 吨，每天可运营 XX 小时。

摘要必须从当前参数计算/拼装，不硬编码。

## 6.5 高级参数

非高频参数收进：

> 展开高级运输参数

普通用户无需默认看到。

---

# 7. 页面四：Step 3 / 运营参数

这是本轮 UX 重构重点。

## 7.1 页面目标

用户应能直观理解：

> “一辆车完成一趟运输需要经历什么，一天为什么能跑这些趟。”

## 7.2 单趟周期可视化

将现有参与单趟周期计算的时间参数按业务流程可视化。

例如：

> 装货 → 去程 → 卸货 → 返程 → 补能

每个环节卡片显示：

- 环节名称
- 当前参数
- 单位
- 可编辑状态

注意：流程节点必须根据当前模型真实参数生成，不允许凭 UI 示例创造新的计算环节。

## 7.3 运营修正参数

独立区域展示：

- 出勤率
- 运营效率
- 年运营天数
- 每日运营时间
- 其他已有关键修正因子

## 7.4 实时计算卡

右侧展示当前关键派生指标，例如模型实际存在的：

- 单趟周期
- 理论日趟次
- 有效日趟次
- 单车日产能
- 单车年运输能力

所有值调用现有计算逻辑。

## 7.5 参数变化反馈

当用户修改关键字段后：

- 输入框正常更新
- 300~500ms debounce 后重算预览
- 有变化的派生指标短暂高亮
- 可展示“原值 → 新值”

不得每输入一个字符就触发昂贵的 AI 请求。

---

# 8. 页面五：Step 4 / 成本收益

## 8.1 页面目标

让业务人员理解：

> 项目收入从哪里来，钱花在哪里。

不再默认展示一张几十行参数表。

## 8.2 收入区

将当前模型已有收入相关参数集中展示。

例如可能包括：

- 运价
- 计价单位
- 年运输量
- 其他收入项

旁边实时展示：

- 预计年收入

## 8.3 成本区

按业务含义将现有成本参数分组。

推荐视觉分组：

- 车辆成本
- 能源成本
- 人员成本
- 维修/运营成本
- 保险税费
- 其他成本

**分组只改变展示，不改变底层参数。**

每组卡片显示：

- 分组名称
- 关键参数摘要
- 当前预计成本
- 展开 / 收起

## 8.4 页面底部实时汇总

固定显示：

- 年收入
- 年成本
- 年利润
- 利润率（若现有模型已有）

仅作为当前参数预览。

---

# 9. 页面六：Step 5 / 确认测算

## 9.1 页面目标

正式计算前建立一道“参数质量检查”机制。

## 9.2 参数完整度

顶部展示：

- 完整度百分比
- 已填写参数数
- 系统计算参数数
- AI 提取参数数
- 待确认参数数

统计口径需要基于真实参数元数据实现。

## 9.3 检查结果分级

### 阻断错误

例如：

- 必填参数为空
- 分母为 0
- 非法负值
- 公式前置条件不满足

必须修改后才能开始测算。

### 风险提醒

例如：

- 参数明显偏离参考范围
- 极端效率假设
- 某成本项为 0

允许用户：

- 修改
- 确认使用当前值

### 正常项

显示绿色确认状态，不需要用户重复操作。

## 9.4 AI 检查

AI 负责：

- 参数语义合理性分析
- 缺失信息提示
- 参数间逻辑冲突解释
- 风险摘要

AI 不负责：

- 改写公式
- 自行改变参数
- 生成正式最终财务结果

## 9.5 主按钮

只有不存在阻断错误时：

> 开始测算

点击后统一调用正式计算流程。

---

# 10. 页面七：测算结果 / 决策页

这是领导演示和业务决策最重要页面。

## 10.1 第一屏必须回答的问题

不滚动页面即可看到核心结果。

核心指标卡根据当前模型已有结果选择，优先：

- 车辆需求
- 年运输能力 / 年运输量
- 年收入
- 年成本
- 年利润
- 利润率
- 投资回收期

建议第一行只放 4~6 个最重要指标。

## 10.2 AI 测算摘要

AI 根据**计算引擎输出结果**生成自然语言摘要。

要求：

- AI 输入必须包含结构化正式测算结果
- AI 只能解释，不得覆盖数值
- 数值展示仍从结果对象读取

摘要内容：

- 当前方案基本结论
- 核心影响因素
- 主要风险
- 值得进一步比较的变量

## 10.3 经营效率

展示真实模型已有的运营指标：

- 单车日趟次
- 单车日产能
- 车辆利用率
- 单位运输成本
- 吨公里成本等

不存在的指标禁止仅为 UI 补造。

## 10.4 成本结构

图表：

- 环形图 / 横向条形图二选一

展示现有成本结果分类及占比。

必须支持 hover 查看：

- 金额
- 占比

## 10.5 现金流

若现有模型已有多年度现金流：

展示：

- 收入
- 成本
- 净现金流 / 利润（按真实模型）

图表不要一次展示过多系列。

## 10.6 风险 / 敏感性

如果当前代码已有敏感性计算，直接复用。

展示方式改为业务语言：

> 当 X 变化 Y 时，车辆需求/利润/回收期发生 Z 变化。

禁止 AI 猜测变化结果。

## 10.7 页面操作

顶部或底部：

- 调整参数
- 创建对比方案
- 导出报告（已有能力则接入）
- 查看完整测算明细

---

# 11. 页面八：方案对比

## 11.1 页面目标

回答：

> 两个方案到底差在哪里？差异由什么导致？

## 11.2 方案选择

顶部：

- 基准方案
- 对比方案
- + 添加方案

首期建议同时对比 2~3 个方案，避免横向无限扩展。

## 11.3 核心指标对比

表格展示真实结果指标。

每一行：

- 指标名称
- 方案 A
- 方案 B
- 差值

差值由程序计算。

## 11.4 参数差异

增加“只看差异参数”开关。

开启后只显示方案之间输入值不同的参数。

这是业务人员判断结果差异来源的重要功能。

## 11.5 AI 差异解释

AI 输入：

- A/B 两套输入参数差异
- A/B 两套正式结果差异

输出：

- 哪些输入发生变化
- 哪些结果受到影响
- 可能的业务原因

不得输出未经计算引擎支持的数字。

---

# 12. 普通模式与专业模式

## 12.1 普通模式

面向：

- 项目经理
- 业务人员
- 运营人员
- 领导查看

特点：

- 业务语言
- 核心字段
- 引导式流程
- 自动计算
- AI 辅助

## 12.2 专业模式

面向模型维护者。

可展示：

- 全量参数
- 参数编码
- 参数来源
- 默认值
- 单位
- 是否必填
- 参数范围
- 公式说明
- 依赖参数
- 中间计算结果
- Excel 映射（如果项目现有）

专业模式必须与普通模式使用同一份参数状态和计算引擎。

---

# 13. AI 助手交互设计

## 13.1 AI 的四类入口

### 入口 A：工作台 AI 创建

用户描述项目 / 上传资料 → AI 提取结构化参数。

### 入口 B：页面级 AI 检查

按钮：

> AI 检查当前步骤

### 入口 C：字段级解释

复杂字段旁增加：

> ?

用户可查看：

- 这个参数是什么
- 为什么需要
- 通常从哪里获取
- 它影响哪些结果

### 入口 D：结果解释

结果页：

> AI 测算摘要

## 13.2 AI 修改参数规则

AI 建议修改参数时，不允许静默修改。

统一交互：

```text
AI 建议：将车辆出勤率由 98% 调整为 92%
原因：……

[应用建议] [保持当前值]
```

只有用户点击“应用建议”后才写入参数。

---

# 14. 表单交互统一规范

## 14.1 输入框

必须明确：

- 参数名称
- 数值
- 单位
- 必填状态

禁止让用户手动在数字中输入单位。

## 14.2 参数来源

可选状态标签：

- 手工填写
- AI 提取
- 系统默认
- 系统计算

不要在普通页面每个字段都展示，hover / 详情时显示即可。

## 14.3 校验

三层校验：

1. 类型校验
2. 合法范围校验
3. 业务逻辑校验

错误信息必须说明如何处理。

不要只显示：

> 参数错误

应该显示：

> 日运营时间必须大于 0 且不超过 24 小时。

## 14.4 自动保存

建议：

- 输入失焦 / debounce 后保存草稿
- 顶部显示保存状态
- 页面切换前确保状态落盘

---

# 15. 状态设计

所有核心页面必须补齐：

- Loading
- Empty
- Error
- Success
- Disabled
- AI analysing
- Calculating

## 15.1 正式测算 Loading

不要只显示 spinner。

建议文案：

> 正在执行项目测算…

下方可展示阶段：

- 校验参数
- 计算运力
- 计算成本收益
- 汇总结果

阶段只在代码真实可拆分时展示，禁止伪进度。

---

# 16. 前端组件拆分建议

可根据现有技术栈调整命名。

建议组件：

```text
AppShell
ProjectHeader
CalculationStepper
ParameterSection
ParameterField
ParameterSourceBadge
RouteScenarioCard
OperationCycleFlow
LiveMetricCard
CostCategoryAccordion
ValidationSummary
ValidationItem
AiAssistPanel
AiSuggestionCard
ResultMetricCard
CostBreakdownChart
CashflowChart
SensitivityCard
ScenarioCompareTable
ParameterDiffTable
AdvancedModeDrawer
```

避免每个页面重复造输入组件和校验逻辑。

---

# 17. 状态管理建议

必须形成单一测算状态源。

示意：

```ts
interface CalculationWorkspace {
  project: ProjectData
  scenario: ScenarioData
  parameters: Record<string, ParameterValue>
  validation: ValidationResult
  previewResult?: CalculationResult
  officialResult?: CalculationResult
  aiContext?: AiContext
  dirty: boolean
}
```

实际类型必须以现有代码为基础，不要照抄示意后形成第二套模型。

关键要求：

> 普通模式、专业模式、AI、方案对比全部围绕同一份参数模型工作。

---

# 18. 计算引擎接入要求

## 18.1 首先审计现有代码

Cursor 在修改前必须先定位：

1. 现有计算入口
2. 参数定义
3. 参数默认值
4. 公式实现
5. 中间计算结果
6. 最终结果对象
7. 敏感性计算
8. 方案数据结构
9. 本地存储/API 逻辑

形成简短映射后再改 UI。

## 18.2 禁止重复计算逻辑

如果已有：

```ts
calculateProject(parameters)
```

新页面必须调用它。

禁止为了实时预览再写：

```ts
const trips = hours / cycleTime
```

导致页面算法与正式算法形成两套口径。

## 18.3 预览结果

实时预览和正式测算原则上使用同一个计算函数。

区别只在：

- 预览：参数变化 debounce 后执行
- 正式测算：完整校验后执行，并固化结果/版本

---

# 19. 方案版本设计

建议数据层明确：

```text
项目
 ├─ 基准方案
 ├─ 方案 B
 └─ 方案 C
```

创建对比方案时：

1. 复制当前方案参数快照
2. 新建 scenarioId
3. 用户只修改需要变化的参数
4. 重新调用同一计算引擎
5. 保存正式结果

禁止方案 B 修改后污染方案 A。

---

# 20. Demo 演示数据要求

当前为了领导演示，可以提供 1~3 个完整种子项目。

但必须满足：

- 输入参数真实进入计算函数
- 结果由函数计算
- 修改关键参数后结果确实变化
- 刷新页面后项目状态可恢复（至少 localStorage / 当前已有持久化机制）
- AI 不可用时，确定性测算流程仍然可以完成

推荐演示项目：

> 云南某大宗运输项目

演示重点不是项目名称，而是完整操作链路。

---

# 21. 推荐领导演示脚本

完整演示控制在 5~8 分钟。

### 场景 1：AI 建项目

输入：

> 我有一个大宗运输项目，年运量……

AI 自动提取。

### 场景 2：确认运输场景

展示距离、载重、运营时长。

### 场景 3：运营测算

修改装卸时间。

现场看到：

- 单趟周期变化
- 日趟次变化
- 单车产能变化

### 场景 4：成本收益

修改电价 / 运价等已有关键变量。

结果实时变化。

### 场景 5：AI 检查

故意设置一个异常参数。

AI 给出风险提醒。

### 场景 6：正式测算

进入决策结果页。

### 场景 7：方案对比

复制方案，修改一个核心参数，展示结果差异。

这样可以完整体现：

> 标准化输入 + 确定性计算 + AI 辅助 + 决策分析。

---

# 22. UI 视觉规范

## 22.1 整体风格

关键词：

- 企业级
- 清晰
- 克制
- 数据可信
- 轻量 AI

避免：

- 大面积渐变
- 过度发光
- 科技大屏风格直接搬到业务后台
- 每个模块都做卡片
- 过多颜色

## 22.2 层级

页面背景使用浅灰；核心工作区域白底。

建议视觉层级：

- 页面标题
- 模块标题
- 关键数字
- 正文
- 辅助信息

## 22.3 颜色语义

沿用项目当前设计系统；若没有统一规范：

- 主操作：品牌主色
- 成功：绿色语义
- 警告：橙色语义
- 错误：红色语义
- AI：可使用主色浅背景 + AI 图标，不单独创造复杂渐变体系

## 22.4 卡片

卡片只用于：

- 核心结果
- 业务分组
- AI 建议
- 风险提示

普通表单不要每个输入框包一张卡片。

---

# 23. 响应式要求

本系统第一阶段以 PC 为主。

最低要求：

- 1366px 可正常使用
- 1440px 主设计基准
- 1920px 不应无限拉宽表单

小于 1200px：

- AI 侧栏允许折叠为 Drawer
- 结果指标卡自动换行

暂不要求移动端完整测算。

---

# 24. 本轮 Cursor 实施顺序

虽然最终全部完成，但代码实施建议按以下顺序，减少返工。

### Phase 0：现有项目审计

先输出：

- 页面路由
- 参数模型
- 计算函数
- 结果模型
- AI 调用位置
- 当前持久化方案

确认后再重构。

### Phase 1：搭建新 Shell

完成：

- 新导航
- ProjectHeader
- Stepper
- 页面路由
- 统一表单组件

### Phase 2：五步测算向导

完成：

- 基本信息
- 运输场景
- 运营参数
- 成本收益
- 确认测算

### Phase 3：结果页

接入真实计算结果。

### Phase 4：方案对比

完成方案复制、独立计算、差异展示。

### Phase 5：AI

将 AI 嵌入：

- 创建
- 补参
- 检查
- 解释

### Phase 6：回归测试

重点确认计算结果与重构前一致。

---

# 25. 必须执行的回归测试

## 25.1 基准案例

至少选择现有系统 3 套已经验证过的输入数据。

分别记录重构前：

- 运力结果
- 成本
- 收入
- 利润
- 现金流
- 其他关键结果

重构后使用完全相同参数重新计算。

要求：

> 所有确定性计算结果与重构前一致（浮点显示精度差异除外）。

## 25.2 参数联动测试

至少测试：

- 距离变化
- 载重变化
- 运营时间变化
- 装卸时间变化
- 电耗/能源参数变化
- 电价变化
- 运价变化
- 出勤/效率变化

实际选择现有模型支持的参数。

## 25.3 异常测试

覆盖：

- 空值
- 0
- 负数
- 极大值
- 小数
- 非数字
- 百分比边界
- 缺失关键参数

## 25.4 方案隔离测试

验证：

- 复制 A 得到 B
- 修改 B
- A 参数不变化
- A 结果不变化
- B 正常重算

## 25.5 AI 降级测试

AI 接口不可用时：

- 可以手动新建项目
- 可以完整填写参数
- 可以执行正式测算
- 可以查看结果
- 只隐藏/提示 AI 能力不可用

AI 不得成为测算主链路的单点故障。

---

# 26. 验收标准

本轮重构完成必须同时满足以下标准。

## 业务体验

- 新用户可以明确知道下一步做什么。
- 普通用户无需理解模型内部字段即可完成测算。
- 关键参数变化后可以看到结果反馈。
- 测算前可以发现缺失和异常参数。
- 结果首页可以直接看到核心经营结论。
- 可以创建至少两个独立方案并对比。

## 计算正确性

- 不修改原有已验证公式口径。
- 同输入的正式结果与重构前一致。
- 页面实时预览与正式计算使用统一计算能力。
- AI 不参与确定性数值计算。

## Demo 完整性

必须能够从头完整演示：

> 新建项目 → 填参数 → AI 检查 → 正式测算 → 查看结果 → 创建方案 → 方案对比

中间不得出现无法继续的死链。

## 工程质量

- 无明显 console error
- 无核心 TypeScript error
- 构建通过
- 页面刷新后状态恢复符合当前项目设计
- AI key 不得暴露在浏览器源码中（如果当前 Demo 采用浏览器直连模型，需要改为安全代理或明确的服务端/云函数方式）

---

# 27. Cursor 执行指令

将以下内容作为本轮开发的总约束：

> 请先完整审计当前项目，不要直接开始重写。找到现有参数模型、计算引擎、计算入口、结果结构、方案结构、AI 调用、路由和持久化逻辑。在确保现有计算公式不被改变的基础上，按照本 PRD 重构前端 UX/UI。
>
> 本次目标不是制作静态效果图，而是让完整业务链路真实可操作。所有测算结果必须继续来自现有确定性计算引擎。页面示例中的字段、数值、公式只用于说明交互，不得覆盖项目真实模型。
>
> 优先复用现有组件和计算代码；发现 PRD 与现有真实数据模型冲突时，以“保留真实计算逻辑 + 实现相同 UX 目标”为原则调整实现，并记录差异。
>
> 重构完成后必须执行全量构建、类型检查、核心链路测试和计算结果回归测试。至少使用 3 套已验证数据比较重构前后结果，输出测试报告。不要仅以“页面可以打开”作为完成标准。

---

# 28. 最终交付物

Cursor 本轮完成后应提供：

1. 新版完整前端代码
2. 页面路由说明
3. 新旧页面映射
4. 新旧参数映射
5. 计算引擎复用说明
6. AI 接入说明
7. 3 套基准案例回归结果
8. 核心链路测试结果
9. 已知问题列表
10. 后续正式生产化改造建议

---

## 最终产品定位

重构后的项目测算系统不应该表现为：

> “把复杂 Excel 做成网页。”

而应该表现为：

> **以标准化业务输入为基础、以确定性测算模型为核心、以 AI 为辅助、面向项目决策的智能测算工作台。**

