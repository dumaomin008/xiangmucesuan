# 项目测算系统——AI业务助手 V1.0

## 一、建设目标

将当前“AI测算结果解读”升级为贯穿项目测算业务流程的 **AI项目测算助手**。

AI不替代现有计算引擎，而是作为业务交互层，负责：
- 理解用户自然语言
- 理解当前项目上下文
- 查询测算结果
- 修改测算参数
- 调用真实计算引擎重新测算
- 分析方案差异
- 识别经营风险
- 输出业务结论和建议

核心原则：

> **数字由计算引擎计算，AI负责理解、调用、分析和解释。**

## 二、产品定位

**产品名称：** AI项目测算助手

**核心定位：** 从“AI结果解读工具”升级为“懂项目、懂参数、懂测算规则、能调用计算能力的AI业务助手”。

## 三、AI整体架构

```text
用户自然语言
      ↓
AI对话入口
      ↓
意图识别
      ↓
项目上下文 + 测算上下文 + 业务规则
      ↓
AI能力路由
 ┌────────────────┐
 │ 项目问答        │
 │ 参数查询        │
 │ 参数修改        │
 │ 重新测算        │
 │ 方案对比        │
 │ 风险分析        │
 │ 经营分析        │
 │ 报告生成        │
 └───────┬────────┘
         ↓
    业务工具调用层
         ↓
 ┌────────────────────┐
 │ 项目数据            │
 │ 测算参数            │
 │ 测算方案            │
 │ Calculation Engine  │
 │ 测算结果            │
 └─────────┬──────────┘
           ↓
       AI生成回答
```

## 四、AI核心能力

### 4.1 AI对话问答

用户可以直接询问当前项目：
- “这个项目月利润多少？”
- “当前车辆配置是多少？”
- “项目收入是多少？”
- “哪个成本最高？”
- “当前IRR是多少？”
- “目前有几个测算方案？”

AI优先从当前项目和当前测算结果读取真实数据，禁止编造项目数据。

### 4.2 项目上下文理解

建议建立统一 `ProjectContext`：

```javascript
{
  projectId,
  projectName,
  customer,
  region,
  owner,
  projectType,
  place,
  tractorDemand,
  trailerDemand
}
```

测算场景进一步注入：

```javascript
{
  scenarioId,
  scenarioName,
  calculationVersion,
  inputs,
  results,
  calculatedAt
}
```

AI上下文：

```text
当前项目
+
当前方案
+
当前参数
+
当前测算结果
+
历史方案
+
业务规则
```

### 4.3 自然语言查询

例如：

> “这个项目一个月能赚多少钱？”

映射到 `monthlyProfit`。

> “现在车辆多少台？”

映射到 `vehicleCount`。

> “现在利润率多少？”

映射到 `profitMargin`。

### 4.4 自然语言修改参数

例如：

> “把电价改成0.65元。”

AI识别：
- 参数：`electricityPrice`
- 原值：当前方案真实值
- 新值：0.65

先反馈：

> 已识别：电价调整为0.65元/kWh，是否重新测算？

用户确认后再执行。

### 4.5 AI调用真实计算引擎

必须调用：

```javascript
calculateProject(input)
```

流程：

```text
AI
 ↓
识别用户要求
 ↓
构造参数变更
 ↓
调用Calculation Engine
 ↓
获得CalculationResult
 ↓
AI解释结果
```

AI不得自行计算：
- 月收入
- 固定成本
- 变动成本
- 能源成本
- 财务成本
- 税务成本
- 月总成本
- 月利润
- 利润率
- 年度现金流
- 累计现金流
- IRR
- 敏感性分析

### 4.6 AI方案创建

例如：

> “帮我做一个低电价方案，电价按照0.65元。”

执行：

```text
当前方案
 ↓
复制方案
 ↓
创建新Scenario
 ↓
修改electricityPrice
 ↓
调用Calculation Engine
 ↓
生成新结果
```

### 4.7 AI方案对比

用户：

> “帮我比较当前方案和低电价方案。”

AI读取两个Scenario的真实结果，对比：
- 月收入
- 月总成本
- 月利润
- 利润率
- IRR
- 累计现金流

AI负责解释差异，不重新计算数字。

### 4.8 AI业务诊断

用户：

> “为什么这个项目利润这么低？”

AI按照业务结构分析：

```text
收入
├── 运输量
├── 运价
└── 运营天数

成本
├── 能源成本
├── 司机成本
├── 车辆成本
├── 维修成本
├── 融资成本
└── 其他成本

现金流
├── 初始投资
├── 运营现金流
└── 回收期
```

### 4.9 AI风险识别

可以识别：
- 月利润/利润率压力
- 能源、车辆、司机、融资等成本压力
- 电价、运价、里程、能耗、车辆数量等参数敏感性
- IRR和前期现金流压力

AI应明确这是基于当前参数和测算结果的分析。

### 4.10 AI敏感性分析

用户：

> “哪些参数对利润影响最大？”

读取已有Sensitivity Engine结果，并解释业务含义。

### 4.11 AI经营建议

根据真实结果提供：
- 成本优化方向
- 车辆配置关注点
- 运价关注点
- 参数风险关注点

## 五、AI工具调用层

建议统一Tool层：

```text
AI
 │
 ├── getProjectContext()
 ├── getScenario()
 ├── getCalculationResult()
 ├── updateScenarioInput()
 ├── calculateProject()
 ├── compareScenarios()
 ├── getSensitivityAnalysis()
 └── generateReport()
```

AI负责决定调用什么工具，工具负责真正执行。

## 六、AI操作确认机制

涉及数据修改时：

```text
用户要求
 ↓
AI识别
 ↓
展示修改内容
 ↓
用户确认
 ↓
执行修改
 ↓
重新计算
```

例如：

> 电价：0.80 → 0.65元/kWh  
> 是否应用并重新测算？

按钮：

```text
[确认并测算] [取消]
```

## 七、AI入口设计

建议采用右侧固定“AI项目测算助手”。

项目详情页重点提供：
- 当前项目情况怎么样？
- 当前项目有哪些核心参数？
- 这个项目有什么风险？

测算输入页：
- 哪些参数最影响利润？
- 当前参数是否存在明显异常？
- 帮我检查一下测算参数。

测算结果页：
- 为什么这个项目利润这么低？
- 帮我分析成本结构。
- 哪些参数最影响利润？
- 帮我找出项目风险。
- 如果电价下降0.1元会怎么样？
- 帮我生成项目汇报结论。

方案对比页：
- 两个方案有什么区别？
- 哪些指标变化最大？
- 帮我解释方案差异。

## 八、AI失败兜底

```text
AI API
 ↓
失败
 ↓
Local Insight Engine
 ↓
继续提供基础业务分析
```

必须保证：
- AI失败不影响测算
- AI失败不影响页面
- AI失败不影响计算结果
- AI失败不能导致白屏
- API Key不能出现在前端

## 九、AI与计算引擎边界

```text
Calculation Engine
负责：
• 所有数字计算
• 财务计算
• IRR
• 敏感性分析
• 结果校验

AI Assistant
负责：
• 自然语言理解
• 业务问答
• 工具调用
• 结果解释
• 风险分析
• 经营建议
```

## 十、V1实施范围

### P0
1. AI对话机器人
2. 项目上下文理解
3. 自然语言查询测算结果
4. 自然语言修改测算参数
5. AI调用真实计算引擎重新测算
6. AI业务分析/风险诊断/方案对比

### P1
7. 自然语言创建测算方案
8. AI敏感性分析解释
9. AI经营建议
10. AI快捷问题
11. AI操作确认机制
12. AI项目汇报结论生成

### P2
13. AI自动生成完整项目测算报告
14. 历史项目智能类比
15. 多项目横向分析
16. 企业知识库业务问答
17. AI Agent自动执行复杂测算任务

## 十一、领导演示主链路

```text
打开项目
 ↓
“帮我分析一下这个项目为什么利润比较低？”
 ↓
AI读取真实结果并分析
 ↓
“如果电价从0.8元降到0.65元呢？”
 ↓
AI识别参数变化
 ↓
确认
 ↓
calculateProject()
 ↓
AI解释真实变化
 ↓
“那如果车辆增加10台呢？”
 ↓
再次调用计算引擎
 ↓
“帮我比较刚才两个方案。”
 ↓
AI读取真实Scenario结果
 ↓
“帮我生成一段给领导汇报的结论。”
```

## 十二、最终产品形态

不是：

```text
测算系统 + 一个AI聊天框
```

而是：

```text
项目测算系统
       │
 ┌─────┴─────┐
 │           │
传统业务     AI业务助手
 │           │
表单/页面    自然语言
 │           │
 └─────┬─────┘
       ↓
Calculation Engine
       ↓
真实测算结果
```

最终目标：

> **用户不需要理解系统每一个字段，也可以通过自然语言完成项目测算、分析和方案比较。**

## 十三、实施原则

1. 不修改现有计算公式。
2. 不让LLM承担核心计算。
3. 所有数字必须来自业务数据或Calculation Engine。
4. AI修改参数必须有确认机制。
5. AI必须知道当前项目和当前方案。
6. AI操作必须可追踪。
7. AI不可用时测算功能必须正常。
8. API Key只允许存在服务端。
9. 优先复用现有LocalStorage和Scenario体系。
10. 优先做Demo完整演示，再扩展生产能力。

## 十四、核心结论

项目测算系统AI建设方向从：

> **“AI帮我解释测算结果”**

升级为：

> **“AI帮我完成项目测算业务。”**

核心链路：

```text
问
↓
理解
↓
查询
↓
修改
↓
确认
↓
真实计算
↓
分析
↓
对比
↓
输出业务结论
```
