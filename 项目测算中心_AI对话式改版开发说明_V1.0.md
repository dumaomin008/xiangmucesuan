# 项目测算中心 AI 对话式改版开发说明 V1.0

> 目标：将当前「项目测算中心」从"数据列表 + 按钮入口"升级为"AI
> 对话驱动 + 可视化结果 + 原有测算能力保留"的工作台。
>
> 本文档用于 Cursor 直接开发。当前阶段以**前端 Demo
> 可完整演示**为第一目标，同时保留后续接真实后端/DeepSeek 的扩展能力。

------------------------------------------------------------------------

## 1. 改版目标

当前页面已经具备： - 测算项目统计 - 测算方案统计 - 待完善测算 -
风险方案 - 最近测算 - 项目测算列表 - 新建测算 - AI 辅助测算 - 查看结果 /
继续测算

本次**不要删除这些业务能力和现有计算逻辑**，而是重构用户入口与信息组织方式。

核心体验改为：

**用户提出业务问题 → AI 理解意图 → 自动调用/读取项目测算数据 →
生成可视化分析 → 给出结论 → 用户继续追问或进入具体测算。**

AI 不只是聊天机器人，而是「项目测算操作入口 + 分析助手 + 结果解释器」。

------------------------------------------------------------------------

# 2. 页面总体结构

建议将「项目测算中心」改为三栏布局：

``` text
┌────────────────────────────────────────────────────────────────────────────┐
│ 项目测算中心                              AI对话测算 | 传统列表视图 | Demo │
├──────────────┬───────────────────────────────────────────┬─────────────────┤
│ 我的对话      │                                           │ 你可以这样问     │
│              │             AI 对话主工作区                 │                 │
│ + 新建对话    │                                           │ 推荐问题         │
│              │  用户问题                                  │                 │
│ 今天          │                                           │ 快捷操作         │
│ - 最近经营分析│  AI回答                                    │                 │
│ - 项目对比    │  ┌ KPI ┐ ┌ KPI ┐ ┌ KPI ┐ ┌ KPI ┐         │ 新建测算         │
│ - 新项目测算  │                                           │ 项目对比         │
│              │  图表 / 表格 / 风险 / AI结论               │ 风险评估         │
│ 最近7天       │                                           │ 导出报告         │
│ ...           │                                           │                 │
│              │  [AI结论与建议]                            │ AI助手能力说明    │
│              │                                           │                 │
│              │  ┌─────────────────────────────────────┐  │                 │
│              │  │ 输入问题...                     发送 │  │                 │
└──────────────┴───────────────────────────────────────────┴─────────────────┘
```

推荐宽度： - 左侧对话历史：220px - 中间主区域：flex: 1 -
右侧辅助区域：260px - 页面最小宽度按现有 PC 管理后台标准处理

------------------------------------------------------------------------

# 3. 顶部区域

保留当前：

**项目测算中心**

副标题修改为：

> 和 AI 一起，快速完成项目测算、方案对比与经营分析

右上角提供：

### 3.1 AI 对话测算

默认选中。

### 3.2 传统列表视图

点击后可切换回当前版本的项目测算列表页面。

注意：

**不要直接删除当前页面。**

当前页面作为传统视图继续保留，避免 AI 改版破坏已有业务能力。

### 3.3 演示状态 / Demo Tools

保留现有 Demo 能力。

------------------------------------------------------------------------

# 4. 左侧「我的对话」

## 4.1 新建对话

顶部按钮：

`+ 新建对话`

点击后： 1. 清空当前聊天上下文 2. 新建 conversationId 3. 中间展示 AI
欢迎页 4. 输入框获得焦点

------------------------------------------------------------------------

## 4.2 对话历史

按照时间分组：

-   今天
-   最近 7 天
-   更早

示例：

``` text
今天

分析最近的测算项目经营情况
10:24

对比杭州和临港项目
09:17

帮我测算一个新项目
08:50

最近7天

分析风险较高的项目
09/17

优化项目方案建议
09/16

生成测算报告
09/15
```

点击历史对话恢复对应聊天内容。

Demo 阶段允许使用 LocalStorage 保存。

建议数据结构：

``` ts
interface Conversation {
  id: string
  title: string
  createdAt: string
  updatedAt: string
  messages: Message[]
}
```

------------------------------------------------------------------------

# 5. AI 欢迎页

新建对话后不要展示空白聊天框。

展示：

``` text
你好，我是项目测算 AI 助手

我可以帮你：

• 从资料中提取项目参数
• 新建项目测算
• 分析项目盈利能力
• 对比不同测算方案
• 识别经营风险
• 做敏感性分析
• 解释测算结果
• 生成经营分析结论
```

下方显示 4 个快捷问题：

-   帮我测算一个新的运输项目
-   分析最近测算项目经营情况
-   对比两个项目的盈利能力
-   哪些项目存在较大风险？

点击即可直接发送。

------------------------------------------------------------------------

# 6. AI 对话主工作区

对话采用 ChatGPT / Copilot 类交互。

## 用户消息

右侧气泡，例如：

> 分析一下最近的测算项目经营情况，并给我关键结论

## AI 消息

左侧显示 AI 图标。

AI 回复不能只输出长文字。

支持以下内容块组合：

``` ts
type AIBlock =
  | TextBlock
  | KPIBlock
  | ChartBlock
  | TableBlock
  | ConclusionBlock
  | RiskBlock
  | ActionBlock
  | CalculationBlock
```

AI 可以一次回答：

``` text
AI说明
↓
KPI指标卡
↓
趋势图 / 对比图
↓
项目明细
↓
AI结论
↓
推荐下一步操作
```

------------------------------------------------------------------------

# 7. AI 可视化结果

这是本次改版重点。

## 7.1 KPI 卡片

例如用户问：

> 分析最近项目经营情况

AI 返回：

  KPI                示例
  ------------ ----------
  测算项目数            3
  测算方案数            7
  预计月收入     ¥456.8万
  平均利润率        11.2%

卡片必须支持： - 主值 - 单位 - 辅助说明 - 上升/下降状态 - tooltip -
点击后查看对应明细（可选）

禁止让 AI 自己随意生成核心财务数字。

**核心数字必须来自项目测算计算引擎 / 已保存 CalculationResult。**

------------------------------------------------------------------------

# 8. 图表组件

建议复用项目当前已有图表库；如无统一方案，可使用 ECharts。

至少实现以下通用组件：

### 8.1 柱状图

用于： - 项目收入对比 - 成本对比 - 利润对比 - 车辆投入对比

### 8.2 折线图

用于： - 收入趋势 - 成本趋势 - 利润趋势 - 利润率趋势

### 8.3 环形图

用于： - 项目状态分布 - 成本结构 - 车辆结构

### 8.4 横向条形图

用于： - 项目盈利能力对比 - 成本项排名 -
风险项目排名（仅按客观指标排序）

### 8.5 敏感性分析图

支持： - 电价变化 - 货量变化 - 运价变化 - 车辆利用率变化 - 空驶率变化

展示变量变化对： - 月利润 - 利润率 - 单车收益

的影响。

------------------------------------------------------------------------

# 9. AI 结果表格

AI 可以在聊天消息内部返回业务表格。

例如：

  ----------------------------------------------------------------------------------------
  项目                           月收入         月成本       月利润       利润率 状态
  ------------------------ ------------ -------------- ------------ ------------ ---------
  临港港区短倒电动化项目      1,722,600   1,512,332.73   210,267.27       12.21% 已测算

  洋山港集装箱短倒项目        1,291,950   1,134,249.55   157,700.45       12.21% 已测算

  杭州城市配送二期项目          172,260     153,357.17    18,902.83       10.97% 已测算
  ----------------------------------------------------------------------------------------

表格右上支持： - 查看全部 - 导出 - 进入项目

金额统一千分位。

正利润正常强调，负利润使用风险色。

------------------------------------------------------------------------

# 10. AI 结论卡

每次涉及分析类问题时，结果底部优先展示：

## AI 结论与建议

例如：

``` text
1. 当前 3 个项目整体处于盈利状态，平均利润率约 11.2%。
2. 临港项目当前月利润最高，可进一步查看成本结构。
3. 某方案在电价上涨情景下出现亏损，需要关注能源成本敏感性。
4. 可进一步模拟“电价 +10%”或“货量 -15%”后的经营结果。
```

注意：

AI 的作用是**解释已经计算出来的数据**。

AI 不得替代确定性财务计算。

------------------------------------------------------------------------

# 11. AI 操作卡

AI 回答完成后，可以返回下一步操作：

``` text
[查看项目详情]
[继续测算]
[方案对比]
[敏感性分析]
[生成分析报告]
```

点击后执行真实页面动作，而不是再发送一条无意义聊天消息。

例如：

`查看项目详情 → /calculation/project/:id`

具体路由请 Cursor
根据当前项目实际路由适配，**不要为了本文档硬改现有路由体系。**

------------------------------------------------------------------------

# 12. 右侧「你可以这样问」

动态推荐问题。

默认：

``` text
分析最近的测算项目经营情况
对比杭州和临港项目的盈利能力
哪些项目存在较大风险，原因是什么？
帮我测算一个新的运输项目
生成本月项目经营分析报告
```

点击问题直接发送。

推荐问题应该根据当前上下文动态变化。

例如已经进入某项目后：

``` text
这个项目最大的成本是什么？
电价上涨10%会怎么样？
货量下降15%还赚钱吗？
需要多少台车？
这个方案和基准方案有什么区别？
```

------------------------------------------------------------------------

# 13. 右侧快捷操作

四个入口：

### 新建测算项目

进入 AI 新建测算流程。

### 项目对比分析

打开项目选择器： - 最少选择 2 个项目/方案 - 选择后 AI 自动生成对比

### 风险方案评估

读取当前项目中风险方案并生成分析。

### 导出数据报表

调用现有导出能力。

------------------------------------------------------------------------

# 14. AI 新建测算流程

用户输入：

> 帮我测算一个新的运输项目

AI 不要一次让用户填写几十个字段。

采用渐进式收集。

例如：

``` text
AI：
可以。你可以：

① 上传项目资料，我帮你自动识别
② 手动告诉我关键条件
③ 从已有项目复制

[上传资料]
[手动输入]
[复制已有项目]
```

------------------------------------------------------------------------

# 15. AI 文件导入

这是必须保留并重点实现的能力。

支持入口： - 输入框附件按钮 - AI 新建测算中的「上传资料」

Demo 至少支持 UI： - Excel - PDF - Word - 图片

上传后显示：

``` text
已上传：
XX项目运输测算资料.xlsx

AI 正在识别项目参数...
```

识别完成后展示结构化结果：

``` text
AI 已识别 18 个项目参数

基础信息
运输距离：126 km
日运输量：1,200 吨
运营天数：30 天

车辆
车辆数量：18 台
单车载重：49 吨

能源
百公里电耗：165 kWh
电价：0.85 元/kWh

发现 3 个参数需要确认：
• 空驶率
• 车辆购置价格
• 司机工资
```

按钮：

`确认并开始测算`

`修改参数`

------------------------------------------------------------------------

# 16. AI 与计算引擎边界

这是开发必须严格遵守的原则。

``` text
用户自然语言
      ↓
DeepSeek
意图识别 / 参数提取
      ↓
结构化参数 JSON
      ↓
现有 Calculation Engine
      ↓
CalculationResult
      ↓
DeepSeek
结果解释 / 风险分析 / 建议
      ↓
前端可视化 Renderer
```

## DeepSeek 可以负责

-   自然语言理解
-   文件内容理解
-   参数抽取
-   缺失参数识别
-   用户问题分类
-   结果解释
-   风险描述
-   方案差异总结
-   生成报告文字

## DeepSeek 不负责

-   月收入最终计算
-   月成本最终计算
-   月利润最终计算
-   利润率最终计算
-   车辆需求量最终计算
-   确定性业务公式

这些必须继续使用项目现有 Calculation Engine。

------------------------------------------------------------------------

# 17. 前端 AI 返回协议

不要让前端解析 DeepSeek 随机 Markdown 来决定 UI。

建议统一 Schema：

``` ts
interface AIResponse {
  message: string

  blocks?: Array<
    | {
        type: 'kpi'
        data: KPIItem[]
      }
    | {
        type: 'chart'
        chartType: 'bar' | 'line' | 'pie' | 'horizontalBar' | 'sensitivity'
        title: string
        data: unknown
      }
    | {
        type: 'table'
        title?: string
        columns: TableColumn[]
        rows: Record<string, unknown>[]
      }
    | {
        type: 'conclusion'
        title: string
        items: string[]
      }
    | {
        type: 'risk'
        data: RiskItem[]
      }
  >

  actions?: AIAction[]

  context?: {
    projectId?: string
    schemeId?: string
    calculationResultId?: string
  }
}
```

------------------------------------------------------------------------

# 18. AI 意图类型

前端/AI service 至少支持：

``` ts
type AIIntent =
  | 'GENERAL_CHAT'
  | 'CREATE_CALCULATION'
  | 'IMPORT_CALCULATION'
  | 'PROJECT_ANALYSIS'
  | 'PROJECT_COMPARE'
  | 'SCHEME_COMPARE'
  | 'RISK_ANALYSIS'
  | 'SENSITIVITY_ANALYSIS'
  | 'CALCULATION_EXPLAIN'
  | 'GENERATE_REPORT'
```

AI 判断 intent 后调用对应 service。

------------------------------------------------------------------------

# 19. 输入框

底部固定输入区域。

Placeholder：

> 请输入您的问题，例如："帮我对比杭州和临港项目的盈利能力"

左侧： - 附件上传

右侧： - 发送

支持： - Enter 发送 - Shift + Enter 换行 - loading - stop
generation（如现有 AI service 支持） - retry - 错误重试

输入框建议最大高度 160px。

------------------------------------------------------------------------

# 20. AI Loading

不要只显示普通 Spin。

展示阶段：

``` text
正在理解你的问题...
正在读取项目数据...
正在执行测算...
正在生成分析...
```

对于 Demo，可根据 intent 映射状态。

------------------------------------------------------------------------

# 21. AI 错误兜底

DeepSeek 调用失败时：

``` text
AI 服务暂时没有响应，但项目测算功能仍可正常使用。

[重新生成]
[进入传统测算]
```

**AI API 失败不能导致整个项目测算中心不可用。**

------------------------------------------------------------------------

# 22. Demo 模式

当前需要给领导演示，因此必须支持稳定 Demo。

建议：

``` ts
VITE_AI_MODE=mock | deepseek
```

### mock

不请求外部 API。

针对预设问题返回固定但基于项目 Demo 数据生成的结构化回答。

至少准备：

1.  最近项目经营分析
2.  项目盈利能力对比
3.  风险项目分析
4.  新项目测算
5.  电价上涨敏感性分析
6.  生成经营分析报告

### deepseek

调用真实 AI service。

演示时即使 DeepSeek 不稳定，也可以立即切换 mock。

------------------------------------------------------------------------

# 23. 不允许破坏现有计算逻辑

Cursor 开发前必须先定位：

-   calculation engine
-   CalculationResult
-   项目数据来源
-   方案数据来源
-   成本计算
-   收入计算
-   利润计算
-   利润率计算
-   车辆数量计算
-   敏感性分析逻辑
-   LocalStorage / mock data
-   当前 DeepSeek service

本次主要改：

**交互层 + AI orchestration + visualization renderer**

不要重新写一套与当前计算逻辑不一致的公式。

------------------------------------------------------------------------

# 24. 当前首页数据迁移方式

原页面顶部：

-   测算项目 3
-   测算方案 7
-   待完善测算 1
-   风险方案 1

不再长期占据首页顶部。

改为 AI 回答中的 KPI。

原：

「最近测算」

改为：

AI 根据问题动态返回「最近测算项目明细」。

原：

「项目测算列表」

保留在： - 传统列表视图 - AI 结果表格 - 查看更多项目

这样减少页面同时堆叠大量重复信息。

------------------------------------------------------------------------

# 25. 建议组件结构

``` text
ProjectCalculationCenter
│
├── CalculationHeader
│
├── ConversationSidebar
│   ├── NewConversationButton
│   └── ConversationList
│
├── AIWorkspace
│   ├── WelcomePanel
│   ├── MessageList
│   │   ├── UserMessage
│   │   └── AIMessage
│   │       └── AIBlockRenderer
│   │           ├── KPIBlock
│   │           ├── ChartBlock
│   │           ├── TableBlock
│   │           ├── ConclusionBlock
│   │           ├── RiskBlock
│   │           └── ActionBlock
│   └── AIComposer
│
└── AIRightPanel
    ├── SuggestedQuestions
    ├── QuickActions
    └── AIAssistantCard
```

------------------------------------------------------------------------

# 26. Service 建议

``` text
services/
├── ai/
│   ├── aiService.ts
│   ├── deepseekService.ts
│   ├── mockAIService.ts
│   ├── intentService.ts
│   └── aiPrompt.ts
│
├── calculation/
│   └── 保留现有逻辑
│
└── project/
    └── 保留现有逻辑
```

AI orchestration：

``` ts
async function handleAIRequest(input: AIRequest) {
  const intent = await detectIntent(input)

  const context = await resolveBusinessContext(intent, input)

  const result = await executeBusinessCapability(intent, context)

  return await buildAIResponse({
    intent,
    context,
    result
  })
}
```

------------------------------------------------------------------------

# 27. UI 视觉规范

沿用当前后台风格，不做完全不同的视觉体系。

建议： - 主色继续使用现有蓝色 - 页面背景浅灰 - 卡片白色 - 圆角 8\~12px -
AI 区域可使用极浅蓝背景 - KPI 卡减少边框，使用轻阴影 -
聊天气泡保持克制 - 金额/利润等数字使用 tabular-nums -
图表禁止使用过多颜色 - 风险色只用于真实风险信息 -
中间聊天区域保持较高信息密度，避免"大段空白 + 超大气泡"

------------------------------------------------------------------------

# 28. 响应式规则

主要面向 PC。

当宽度不足时： 1. 右侧推荐区域可折叠 2. 左侧历史对话可折叠 3. 中间 AI
工作区始终优先 4. 图表自动变为单列

------------------------------------------------------------------------

# 29. 首屏默认演示

为了让领导打开页面就能理解价值：

进入项目测算中心默认可以自动展示一条 Demo 会话：

用户：

> 分析一下最近的测算项目经营情况，并给我关键结论

AI：

1.  一句话说明
2.  4 个 KPI
3.  收入/月利润趋势图
4.  项目状态环形图
5.  最近项目明细
6.  AI 结论与建议
7.  「生成分析报告」等操作按钮

这样首屏不是一个空聊天机器人。

------------------------------------------------------------------------

# 30. 开发顺序

Cursor 可以按以下顺序一次性实现：

1.  保留当前传统列表页
2.  新建 AI 对话布局
3.  Conversation Sidebar
4.  Message UI
5.  AIBlockRenderer
6.  KPI
7.  Chart
8.  Table
9.  Conclusion
10. Action
11. Suggested Questions
12. Quick Actions
13. AI 新建测算
14. 文件导入入口
15. DeepSeek service 对接
16. Calculation Engine 接入
17. Mock AI
18. LocalStorage 会话历史
19. Loading / Error / Empty 状态
20. 全流程测试

------------------------------------------------------------------------

# 31. Cursor 开发硬性要求

## 必须

-   先审计现有项目再修改
-   复用现有组件库和设计语言
-   保留当前计算引擎
-   保留当前项目/方案数据
-   保留传统列表视图
-   AI 输出支持结构化可视化
-   DeepSeek 与 mock 可切换
-   AI 失败不影响普通测算
-   所有金额与当前 CalculationResult 保持一致
-   真实测算结果不得由 AI 猜测
-   不新增无业务依据的计算参数
-   不修改已经验证通过的公式，除非发现明确 Bug
-   所有新增 TypeScript 类型完整定义
-   不允许大面积使用 `any`
-   完成后执行现有 lint / typecheck / build / test

## 禁止

-   删除现有测算功能
-   为做聊天页面重写业务计算
-   将 API Key 写死在源码
-   让浏览器端永久暴露正式 DeepSeek API Key
-   AI 返回随机数字覆盖真实测算结果
-   只做一个聊天框而没有业务操作能力
-   把所有 AI 输出都当 Markdown 字符串展示

------------------------------------------------------------------------

# 32. 验收场景

## 场景 A：经营分析

用户：

> 分析最近的测算项目经营情况

预期： - AI 识别 PROJECT_ANALYSIS - 读取真实 Demo 项目数据 - 输出 KPI -
输出图表 - 输出项目表格 - 输出结论 - 金额与当前页面一致

------------------------------------------------------------------------

## 场景 B：项目对比

用户：

> 对比杭州城市配送二期项目和临港港区短倒电动化项目

预期： - 输出两项目核心指标 - 收入 - 成本 - 利润 - 利润率 -
车辆数量（如数据存在） - 成本结构（如数据存在） - AI 解释主要差异 -
支持进入项目

------------------------------------------------------------------------

## 场景 C：敏感性分析

用户：

> 如果临港项目电价上涨 10%，利润会怎么样？

预期：

``` text
自然语言
↓
AI提取：
project = 临港
variable = electricityPrice
change = +10%
↓
Calculation Engine
↓
新的 CalculationResult
↓
前端展示：
原方案 vs 新方案
```

AI 只负责解释变化原因。

------------------------------------------------------------------------

## 场景 D：资料测算

用户上传项目资料。

预期： - 文件展示 - AI 参数抽取 - 参数确认 - 缺失项提示 - 用户补充 -
调用 Calculation Engine - 生成测算结果 - 生成可视化结论

------------------------------------------------------------------------

## 场景 E：DeepSeek 失败

模拟 API 超时。

预期： - 页面不崩溃 - 展示错误提示 - 可以重新生成 - 可以切传统测算 -
已有项目数据不丢失

------------------------------------------------------------------------

# 33. 最终交付要求

Cursor 完成后，请输出：

``` text
1. 修改文件清单
2. 新增文件清单
3. 页面路由变化
4. AI 调用链路
5. Calculation Engine 调用链路
6. Mock / DeepSeek 切换方式
7. 环境变量清单
8. 已完成测试
9. 未完成项
10. 已知风险
```

并执行：

``` bash
npm run lint
npm run typecheck
npm run test
npm run build
```

如果项目实际 scripts 名称不同，以 package.json 为准。

------------------------------------------------------------------------

# 34. 最终产品定位

本次改版不是：

> 给项目测算系统增加一个聊天机器人。

而是：

> **将 AI 作为项目测算中心新的交互入口。**

最终用户不需要理解复杂的系统菜单和大量字段，可以直接说：

> "帮我测算这个项目。"

> "为什么这个项目利润这么低？"

> "如果电价涨 10% 会怎么样？"

> "杭州和临港哪个成本差异最大？"

> "把这个 Excel 导进去重新算一下。"

系统负责完成：

**理解 → 取数 → 测算 → 可视化 → 解释 → 下一步操作。**

同时，底层仍坚持：

**AI 负责理解和解释，Calculation Engine 负责确定性计算。**
