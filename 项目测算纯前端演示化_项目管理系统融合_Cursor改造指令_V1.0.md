# 项目测算纯前端演示化 + 项目管理系统融合 Cursor 改造指令 V1.0

> 目标：在**没有业务后端、数据库、Prisma/SQLite
> 服务**的情况下，项目管理系统 Demo
> 中仍可进入"项目测算"，完成真实参数录入、真实计算、方案保存、方案对比和
> AI 解读。\
> 原则：**页面可以 Mock，核心测算结果绝对不能
> Mock。计算口径必须与当前项目测算仓库及 Excel Golden Test 保持一致。**

## 1. 本轮改造范围

本轮涉及两个工程：

1.  **项目管理系统 Demo**
    -   当前为 Vanilla JavaScript 静态 SPA。
    -   核心文件：`index.html`、`app.js`、`project-data.js`、`styles.css`、`server.mjs`。
    -   当前已有项目列表和 `#/projects/{projectId}` 项目详情路由。
    -   作为最终演示的"主壳"。
2.  **项目测算工程**
    -   GitHub：`dumaomin008/xiangmucesuan`
    -   当前核心测算能力继续作为唯一业务口径。
    -   将演示所需计算能力改造成浏览器端可直接运行的纯函数计算引擎。
    -   原正式后端代码不得粗暴删除。

本轮**不做正式生产系统合库**，先完成领导演示所需的一体化前端 Demo。

------------------------------------------------------------------------

## 2. 最终演示链路

``` text
登录
  ↓
项目管理
  ↓
项目列表
  ↓
项目详情
  ↓
【项目测算】
  ↓
自动继承当前项目上下文
  ↓
测算方案 / 参数录入
  ↓
浏览器端真实计算
  ↓
收入 / 成本 / 利润 / 现金流 / IRR 等结果
  ↓
方案对比
  ↓
AI 解读
  ↓
保存方案
  ↓
返回项目详情
```

要求整个过程中即使断开项目测算业务后端，除 AI 外的核心测算仍然正常工作。

------------------------------------------------------------------------

## 3. 架构原则

### 3.1 双模式保留

不要把现有正式架构删除，新增 Demo 运行模式：

``` text
正式模式：
UI → API → 后端 Calculation Engine → Database

演示模式：
UI → Browser Calculation Engine → LocalStorage
```

建议统一配置：

``` ts
APP_MODE = "demo" | "server"
CALC_MODE = "browser" | "api"
```

演示默认：

``` ts
APP_MODE = "demo"
CALC_MODE = "browser"
```

### 3.2 四类能力划分

  能力                       Demo 处理
  -------------------------- ------------------------------------
  核心测算公式               必须真实迁入浏览器端
  项目、方案、历史记录       LocalStorage / Mock Repository
  Prisma、SQLite、业务 API   Demo 模式不得成为运行依赖
  AI                         独立在线能力，失败不得阻塞基础测算

------------------------------------------------------------------------

## 4. 核心测算引擎改造

### 4.1 禁止重新发明公式

必须从现有项目测算仓库中抽取已经验证的计算逻辑。

不得：

-   为方便前端而简化公式；
-   修改取整规则；
-   修改空值和 0 值含义；
-   修改单位；
-   修改百分比口径；
-   修改税务、财务、折旧、能耗等计算口径；
-   用页面展示结果反推公式；
-   用 Mock 结果替代真实计算。

### 4.2 建议目录

在项目测算工程中建立浏览器安全的计算核心，例如：

``` text
src/
  calculation/
    core/
      types.ts
      normalize.ts
      capacity.ts
      revenue.ts
      fixed-cost.ts
      variable-cost.ts
      energy.ts
      finance.ts
      tax.ts
      profit.ts
      cashflow.ts
      irr.ts
      sensitivity.ts
      validation.ts
      calculate.ts
    adapters/
      browser.ts
      api.ts
```

`core` 内：

-   不允许 Prisma；
-   不允许 Node `fs`；
-   不允许数据库调用；
-   不允许 Route Handler；
-   不允许直接读取环境中的服务端 Secret；
-   输入为普通 JSON/TypeScript 对象；
-   输出为普通可序列化对象；
-   相同输入必须得到相同输出。

### 4.3 统一入口

建立唯一计算入口：

``` ts
calculateProject(input): CalculationResult
```

所有结果页、方案对比、AI
上下文都必须读取该入口的结果，禁止各页面自行复制公式。

------------------------------------------------------------------------

## 5. 精度与边界规则

必须完整继承当前计算引擎行为，尤其检查：

-   Decimal/高精度金额运算；
-   元、万元之间的换算；
-   km 与能耗单位；
-   月/年换算；
-   百分比输入与内部小数；
-   四舍五入时机；
-   车辆数等整数取整；
-   0 值是否表示"真实为 0"还是"未填写"；
-   除数为 0；
-   空数组；
-   空线路；
-   空载/满载判断；
-   负利润；
-   IRR 不可求解；
-   极端电价；
-   极端里程；
-   极端融资比例。

**显示层格式化与计算层精度必须分离。**

------------------------------------------------------------------------

## 6. Golden Test 一致性

必须建立前端计算引擎回归测试。

至少选择：

1.  原 Excel 基准案例；
2.  当前后端已有测试案例；
3.  正常盈利案例；
4.  亏损案例；
5.  0 值/空值边界案例；
6.  高能耗/高电价案例；
7.  融资场景；
8.  多线路/多段场景；
9.  IRR 场景；
10. 敏感性分析场景。

同一输入至少比较：

``` text
Excel结果
后端现有结果
浏览器计算引擎结果
```

金额、比例、IRR
等按当前项目已有容差规则验收，不允许人为放宽容差掩盖错误。

------------------------------------------------------------------------

## 7. Demo 数据层

新增 Demo Repository，页面不直接操作 LocalStorage。

建议：

``` text
src/demo/
  repository/
    projectRepository
    scenarioRepository
    parameterRepository
```

统一接口：

``` ts
getProject()
listScenarios()
getScenario()
saveScenario()
duplicateScenario()
deleteScenario()
```

演示模式底层使用 LocalStorage；正式模式未来可切回 API。

建议 Key：

``` text
pm_demo_projects
pm_demo_calc_scenarios
pm_demo_calc_drafts
pm_demo_calc_preferences
```

必须考虑 LocalStorage 数据版本：

``` json
{
  "schemaVersion": 1,
  "data": {}
}
```

避免后续字段变化导致 Demo 无法启动。

------------------------------------------------------------------------

## 8. 项目管理系统融合

当前项目管理 Demo 已存在：

``` text
#/projects/{projectId}
```

### 8.1 项目详情增加入口

在项目详情 Header 的操作区，在"编辑项目"附近增加：

**项目测算**

建议路由：

``` text
#/projects/{projectId}/calculation
```

项目测算不是孤立一级系统，而是当前项目的业务能力。

同时可在左侧业务管理区域增加：

**项目测算中心**

用于跨项目查看全部测算方案。

### 8.2 项目上下文

从项目管理 Demo 向测算模块传递统一 `ProjectContext`：

``` ts
{
  projectId,
  projectName,
  customer,
  region,
  owner,
  projectType,
  place,
  tractorDemand,
  trailerDemand
}
```

规则：

-   项目 ID 是关联主键；
-   不允许根据项目名称关联；
-   进入项目测算后自动带入可复用信息；
-   测算专属参数由用户补充；
-   项目基础信息与测算参数在 UI 上区分。

------------------------------------------------------------------------

## 9. 测算方案模型

一个项目允许多个测算方案：

``` text
项目
 ├─ 基准方案
 ├─ 方案A
 ├─ 方案B
 └─ 电价上涨情景
```

建议数据结构：

``` ts
{
  id,
  projectId,
  name,
  version,
  status,
  createdAt,
  updatedAt,
  inputs,
  results,
  calculationVersion
}
```

必须保存：

-   输入快照；
-   计算结果快照；
-   计算引擎版本；
-   创建/更新时间。

复制方案时复制输入，但生成新的方案 ID。

------------------------------------------------------------------------

## 10. 页面要求

项目测算至少保留/完成：

``` text
测算概览
基础参数
线路/运营参数
运力测算
收入测算
成本测算
盈利分析
现金流/IRR
敏感性分析
方案对比
AI 分析
```

不要为了融合项目管理系统而删掉已经开发完成且可演示的核心能力。

### 10.1 UI 统一

最终视觉以项目管理系统 Demo 的设计规范为母版：

-   专业可信；
-   克制；
-   高信息密度；
-   轻科技；
-   数据驱动；
-   蓝/绿/中性色；
-   小圆角；
-   弱边框；
-   轻阴影；
-   避免大量紫色 AI SaaS 风格；
-   避免 Card 套 Card。

测算模块需要统一：

-   Header；
-   Sidebar；
-   面包屑；
-   按钮；
-   表格；
-   输入框；
-   字号；
-   间距；
-   状态标签。

不要出现明显"从另一个系统跳过去"的视觉断层。

------------------------------------------------------------------------

## 11. AI 能力

AI 与核心计算必须解耦。

正确链路：

``` text
用户输入
 ↓
前端真实计算
 ↓
结构化 CalculationResult
 ↓
AI 接收“输入 + 结果 + 规则摘要”
 ↓
生成解释/风险/建议
```

AI **不得负责核心数字计算**。

AI 不可用时：

-   测算仍然完成；
-   所有数字正常显示；
-   只在 AI 区域提示暂不可用；
-   不允许整个页面报错。

前端代码中禁止暴露真实大模型 Secret/API Key。若领导演示必须使用真实
AI，应使用单独的轻量代理/安全网关；纯静态部署时不要把 Secret
打包进浏览器。

------------------------------------------------------------------------

## 12. 项目管理 Demo 具体改造点

当前静态 Demo 的 `app.js` 已包含 `projectDetail(id)` 和项目详情 Header。

第一轮至少修改：

``` text
app.js
project-data.js
styles.css
```

新增：

1.  `#/projects/{id}/calculation` 路由；
2.  项目详情"项目测算"按钮；
3.  项目上下文构造函数；
4.  测算页面容器；
5.  返回项目详情能力；
6.  测算方案状态提示；
7.  必要的样式。

不要破坏：

-   `/projects`
-   `/projects/new`
-   `/projects/{id}`
-   `/projects/{id}/edit`
-   客户管理
-   车辆管理
-   当前权限 Demo

------------------------------------------------------------------------

## 13. 不推荐 iframe 作为最终方案

可临时用于 P0 验证，但最终领导演示版不以 iframe 为目标。

原因：

-   UI 割裂；
-   路由返回体验差；
-   项目上下文传递麻烦；
-   LocalStorage 域隔离风险；
-   AI 与测算状态难统一；
-   容易被看出是两个 Demo 拼接。

目标应是统一壳层下的业务模块融合。

------------------------------------------------------------------------

## 14. 静态部署要求

最终基础 Demo 应支持：

``` text
静态资源服务器
        ↓
index.html
JS/CSS/assets
        ↓
项目管理
        ↓
项目测算
        ↓
Browser Calculation Engine
```

要求：

-   无数据库也可运行；
-   无 Prisma 也可运行；
-   无业务后端也可计算；
-   刷新后方案不丢失（LocalStorage）；
-   AI 故障不影响计算；
-   项目上下文不丢失；
-   静态托管路径下路由可正常访问。

Hash Router 可继续保留，降低静态部署风险。

------------------------------------------------------------------------

## 15. 演示数据

至少准备 3 个完整项目：

1.  正常盈利项目；
2.  盈利边缘项目；
3.  明显亏损项目。

每个项目至少准备：

-   1 个基准方案；
-   1 个对比方案。

领导现场修改以下参数时必须实时重新计算：

-   运量；
-   车辆数；
-   里程；
-   载重；
-   运价；
-   电价；
-   能耗；
-   司机成本；
-   车辆购置价格/租赁成本；
-   融资参数。

结果链路必须同步变化：

``` text
收入
成本
单车经济性
利润
利润率
现金流
IRR（适用时）
敏感性结果
```

------------------------------------------------------------------------

## 16. 自动化验收

完成后至少执行：

### A. 静态检查

-   build/check 无错误；
-   浏览器 console 无阻断性错误；
-   不存在运行时 Prisma 依赖；
-   不存在浏览器调用 SQLite；
-   不存在浏览器 Secret。

### B. 业务链路

``` text
项目列表
→ 项目详情
→ 项目测算
→ 新建方案
→ 修改参数
→ 执行测算
→ 查看结果
→ 保存
→ 复制方案
→ 修改关键参数
→ 方案对比
→ AI分析
→ 返回项目详情
→ 再次进入
→ 历史方案仍存在
```

### C. 计算验证

必须输出测试报告，至少包括：

  Case         Excel   原后端   Browser Engine   差异 结论
  ---------- ------- -------- ---------------- ------ ------
  基准案例                                            
  盈利案例                                            
  亏损案例                                            
  边界案例                                            

不能只写"测试通过"，必须有关键指标实际值。

------------------------------------------------------------------------

## 17. Cursor 实施顺序

严格按顺序执行，不要一次性重构全部工程。

### Phase 1：计算引擎浏览器化

-   找出所有核心公式；
-   消除计算核心中的服务端依赖；
-   建立统一 `calculateProject()`；
-   建立 Golden Test；
-   确认浏览器与后端结果一致。

**Phase 1 未通过，不进入下一阶段。**

### Phase 2：Demo 数据层

-   LocalStorage Repository；
-   项目/方案 Mock；
-   方案保存、复制、删除；
-   数据版本管理。

### Phase 3：项目管理融合

-   项目详情增加测算入口；
-   路由打通；
-   ProjectContext；
-   返回链路；
-   测算方案与项目关联。

### Phase 4：UI 统一

以项目管理 Demo Design Spec 为准统一视觉，不改变业务公式。

### Phase 5：AI

将结构化测算结果提供给 AI；AI 故障降级。

### Phase 6：全量验收

-   静态部署测试；
-   业务链路测试；
-   Golden Test；
-   边界测试；
-   演示脚本测试。

------------------------------------------------------------------------

## 18. Cursor 禁止事项

本轮严禁：

1.  删除原后端正式能力；
2.  擅自修改业务公式；
3.  用 Mock 数字冒充计算结果；
4.  把 API Key 放前端；
5.  为赶 Demo 删除 Golden Test；
6.  大范围重写项目管理 Demo；
7.  将项目名称作为关联键；
8.  将 AI 当作计算器；
9.  iframe 作为最终融合方案；
10. 未验证结果一致性就宣布"纯前端迁移完成"。

------------------------------------------------------------------------

## 19. 完成定义 DoD

只有以下条件全部满足，才允许标记本轮完成：

-   [ ] 无项目测算业务后端时仍能完成完整测算；
-   [ ] 核心公式为真实计算；
-   [ ] Browser Engine 与 Golden Test 一致；
-   [ ] 项目详情可以直接进入项目测算；
-   [ ] 项目上下文自动带入；
-   [ ] 一个项目支持多个方案；
-   [ ] 方案支持保存和再次打开；
-   [ ] 支持方案对比；
-   [ ] 修改参数后结果真实联动；
-   [ ] AI 与计算解耦；
-   [ ] AI 失败不影响测算；
-   [ ] UI 与项目管理系统基本一致；
-   [ ] 可静态部署；
-   [ ] Console 无阻断性错误；
-   [ ] 输出最终测试报告。

------------------------------------------------------------------------

## 20. Cursor 完成后必须回传

不要只回复"已完成"。请回传：

1.  修改文件清单；
2.  新增文件清单；
3.  核心计算迁移说明；
4.  被替换/Mock 的 API 清单；
5.  LocalStorage 数据结构；
6.  项目上下文映射表；
7.  Golden Test 结果；
8.  静态部署测试结果；
9.  已知问题；
10. 未完成事项；
11. 启动命令；
12. 演示访问路径；
13. 最终 Git commit SHA。

------------------------------------------------------------------------

# 最终目标

这不是一个"页面能打开"的 Demo。

最终必须达到：

> **项目管理系统负责项目业务上下文，项目测算负责真实经济性计算，AI负责解释和辅助决策；即使没有项目测算业务后端，领导现场修改参数后，所有核心测算结果仍能真实、正确、可重复地计算出来。**
