# 当前 DEMO 默认可零配置运行（本地引擎解读）。
# Key 只放本机环境变量，禁止提交真实值，禁止写入 JS / HTML / URL。

# AI_PROVIDER=
# AI_BASE_URL=
# AI_API_KEY=
# AI_MODEL=

# 静态前端指向已部署 API 的源（不含路径、不含密钥）。同源留空。
# 由 server.mjs 注入 demo-config.js；纯静态托管时直接改 demo-config.js。
# DEMO_API_BASE_URL=
# 允许跨域调用 AI / 导入接口的前端 Origin，逗号分隔。不要使用 *。
# DEMO_CORS_ORIGINS=

# DOCUMENT_PARSER_MODE=demo

# 对话测算模式。本 Demo 不是 Vite，不读取 VITE_AI_MODE。
# 页面默认 mock；可在「演示状态」切换，或写入浏览器 localStorage：pm-ai-mode=mock|deepseek
# deepseek 只请求服务端 /api/demo-ai/explain，浏览器不保存 API Key。
# DEMO_AI_MODE=mock
